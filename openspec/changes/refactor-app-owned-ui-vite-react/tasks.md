## 1. Implementation

- [x] 1.1 Replace the legacy app-owned DOM script entry with a Vite React
  TypeScript source tree.
- [x] 1.2 Keep bridge calls provider-neutral through declared application UI
  bridge capabilities.
- [x] 1.3 Preserve app-owned session mementos, replay hydration, websocket
  streaming, and markdown presentation inside the application package.
- [x] 1.4 Build a static `ui/dist/index.html` bundle and update the application
  manifest to point at that bundle.
- [x] 1.5 Verify the app-owned bundle with local build, UI contract tests, and
  runtime static asset checks.
