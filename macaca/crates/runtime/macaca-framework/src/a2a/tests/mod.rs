//! Contract tests for the A2A module tree.
//!
//! Tests are partitioned by concern (wire types, formatter Adapter, resolver Strategy)
//! so each file stays under the 500-line OS-layer limit.

mod fixtures;
mod formatter_tests;
mod resolver_tests;
mod types_tests;
