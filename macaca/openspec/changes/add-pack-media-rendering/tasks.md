## 1. Supplier API Research And Scope

- [x] 1.1 Re-read architecture governance, microkernel boundaries, serviceization allowlist, design patterns, OpenSpec rules, and the industrial catalog umbrella proposal before implementation.
- [x] 1.2 Study Skia for canvas-style 2D drawing, paths, text, images, filters, surfaces, GPU/CPU execution, Skottie animation, and failure/resource behavior.
- [x] 1.3 Study Cairo for vector drawing, image/PDF/SVG-like surfaces, text/path rendering, surface lifecycle, and deterministic output behavior.
- [x] 1.4 Study Canvas 2D and WHATWG Canvas for bitmap drawing surfaces, shapes, text, images, compositing, dimensions, origin-clean constraints, and export behavior.
- [x] 1.5 Study WebGPU for GPU device limits, shaders, pipelines, buffers, textures, command queues, adapter/device availability, and resource/error behavior.
- [x] 1.6 Study ImageMagick, librsvg, Lottie/Skottie, and Headless Chrome/CDP Page capture for image conversion, static SVG rendering, vector animation frames, viewport snapshots, remote resource constraints, and export artifacts.
- [x] 1.7 Produce a supplier capability comparison memo mapping CPU engines, GPU engines, browser renderers, SVG renderers, animation renderers, image processors, font providers, asset stores, and export concepts into Macaca provider-neutral rendering DTOs and commands.
- [x] 1.8 Define explicit non-goals for concrete provider implementation, UI renderer/design-tool/game/video/PDF/office workflows, raw provider pass-through, raw pixel/vector observability, and provider/engine/shader/template/font/URL-specific routing.
- [x] 1.9 Record GitNexus CRITICAL/HIGH findings as memo only before implementation commits.

## 2. Contract, Descriptor, And DTOs

- [x] 2.1 Define `pack.media.rendering.v1` descriptor metadata: pack id, family, lifecycle, stability, engine classes, raster support, vector support, animation support, preview support, export support, surface formats, color profiles, viewport limits, CPU/GPU classes, shader support, script policy support, remote asset support, font support, auth modes, command schemas, permission scopes, policy templates, resource budgets, approval requirements, data-governance class, SDK metadata, documentation link, compatibility, and diagnostics.
- [x] 2.2 Define provider-neutral DTOs for `RenderingScope`, `RenderingProviderCapability`, `RenderSourceHandle`, `RenderTemplateMetadata`, `SceneGraphSummary`, `RenderViewport`, `RenderSurfaceProfile`, `RenderAssetHandle`, `RenderFontReference`, `RenderPlan`, `RenderFramePlan`, `RenderAnimationPlan`, `RenderPreviewPlan`, `RenderExportPlan`, `RenderJobStatus`, and `RenderArtifactHandle`.
- [x] 2.3 Define typed command/result DTOs for `rendering.inspect_provider`, `rendering.import_source_request`, `rendering.open_source`, `rendering.inspect_template`, `rendering.inspect_scene_graph`, `rendering.validate_assets`, `rendering.plan_render`, `rendering.render_request`, `rendering.plan_frame`, `rendering.frame_request`, `rendering.plan_animation`, `rendering.animation_request`, `rendering.plan_preview`, `rendering.preview_request`, `rendering.plan_export`, `rendering.export_request`, `rendering.inspect_job`, `rendering.cancel_job`, and `rendering.get_artifact_handle`.
- [x] 2.4 Define typed success, paged, partial, asynchronous, denied, unavailable, unsupported, conflict, stale-version, schema-mismatch, format-unsupported, asset-denied, font-denied, script-denied, network-denied, shader-denied, gpu-unavailable, render-denied, export-denied, write-denied, artifact-denied, quota, timeout, cancellation, approval-required, and failure DTOs.
- [x] 2.5 Define stable descriptor hashing, provider capability hashing, source version hashing, template metadata hashing, scene graph summary hashing, viewport hashing, surface profile hashing, asset/font validation hashing, render plan hashing, frame plan hashing, animation plan hashing, preview plan hashing, export plan hashing, job status hashing, artifact handle hashing, event cursor hashing, and redaction metadata.
- [x] 2.6 Add descriptor and DTO compatibility tests for valid descriptors, rejected invalid descriptors, stable hashes, schema evolution, source handles, template metadata, scene summaries, viewports, surfaces, asset/font references, render plans, frame plans, animation plans, preview plans, export plans, jobs, artifacts, redaction profiles, and serde compatibility.

## 3. Admission, Permission, Policy, Resource, Entitlement, And Approval

- [x] 3.1 Implement manifest declaration validation for required and optional `pack.media.rendering.v1` declarations.
- [x] 3.2 Implement permission validation for `rendering.provider.inspect`, `rendering.source.import`, `rendering.source.open`, `rendering.template.read`, `rendering.scene.read`, `rendering.asset.validate`, `rendering.render`, `rendering.frame`, `rendering.animation`, `rendering.preview`, `rendering.export`, `rendering.job.read`, `rendering.job.cancel`, and `rendering.artifact.read`.
- [x] 3.3 Implement provider/source/template/scene/asset/font/viewport/surface/job/artifact/export scope checks for declared sources, private assets, licensed fonts, generated assets, denied scopes, stale handles, and external delivery.
- [x] 3.4 Implement policy checks for source format, template version, scene graph bounds, viewport dimensions, surface profile, output format, color profile, metadata stripping, script policy, remote URL policy, asset handle availability, font reference availability, shader policy, animation duration, frame count, deterministic preview settings, artifact retention, and output redaction.
- [x] 3.5 Implement resource reservation for source size, node count, layer count, path count, text count, image count, asset count, font count, viewport pixel count, output dimensions, frame count, animation duration, filter/effect count, shader count, provider quota, network transfer, timeout, CPU/GPU class, memory, storage, artifact size, and retained snapshots.
- [x] 3.6 Implement entitlement checks and structured unavailable/denied diagnostics for missing provider, disabled pack, missing credential reference, missing source permission, unsupported format/surface/engine/shader/animation/export support, denied script/network/font/asset access, disabled GPU, missing entitlement, provider quota, and host resource denial.
- [x] 3.7 Implement approval behavior for private templates, licensed fonts, copyrighted assets, remote URL fetching, script-enabled rendering, GPU/shader execution, external delivery, metadata stripping, artifact publication, and operations that publish outputs.
- [x] 3.8 Add tests proving denied, validation, quota, unavailable, conflict, stale-version, schema-mismatch, format-unsupported, asset-denied, font-denied, script-denied, network-denied, shader-denied, gpu-unavailable, render-denied, export-denied, write-denied, artifact-denied, unsupported, timeout, cancellation, and approval-required paths do not call concrete providers, fetch remote resources, execute scripts, use fonts/assets, render pixels, export artifacts, publish outputs, or expose raw source/pixel/vector data.

## 4. Service Provider And Runtime Integration

- [x] 4.1 Implement or bind the rendering service provider behind the service runtime; do not construct rendering providers from SDK, shell, kernel, or application code.
- [x] 4.2 Add a deterministic unavailable provider that returns typed unavailable/unsupported diagnostics and complete discovery metadata.
- [x] 4.3 Add mock provider support for provider inspection, source import/open, template inspection, scene graph inspection, asset/font validation, render planning/request, frame planning/request, animation planning/request, preview planning/request, export planning/request, job inspection, cancellation, artifact handles, health, and provider capability inspection.
- [x] 4.4 Add lifecycle, health, snapshot, shutdown, timeout, cancellation, bounded paging, asynchronous job/artifact behavior, source version freshness, stale-version diagnostics, schema/format/surface mismatch diagnostics, asset/font/script/network/GPU diagnostics, artifact retention, and rate-limit diagnostics.
- [x] 4.5 Add Strategy implementations for provider adapters, CPU renderers, GPU renderers, browser renderers, SVG renderers, animation renderers, image processors, font resolvers, asset providers, export providers, redaction, and unavailable behavior.
- [x] 4.6 Add explicit state machines for rendering jobs, including planned, queued, running, cancelling, cancelled, completed, failed, timed_out, unavailable, and replay states.
- [x] 4.7 Add side-effect safety support for idempotency keys, provider state validation, source version preconditions, script/network decisions, asset/font validation state, approval state, artifact retention, job cancellation, and non-mutating plan commands.
- [x] 4.8 Add provider capability reporting for available, degraded, preview, unavailable, unsupported, retired, format-limited, surface-limited, animation-limited, preview-limited, export-limited, script-disabled, network-limited, font-limited, asset-limited, GPU-limited, CPU-limited, and quota-limited states.

## 5. SDK, WASM ABI, Application Framework, And Examples

- [x] 5.1 Extend SDK discovery for `pack.media.rendering.v1` with command schemas, engine classes, raster/vector support, animation support, preview support, export support, surface formats, viewport limits, CPU/GPU classes, shader policy, script/network/asset/font policy support, examples, availability, diagnostics, documentation link, provider class, capability hash, compatibility, and redaction profile.
- [x] 5.2 Extend application admission so required declarations block readiness when unavailable and optional declarations degrade explicitly with effective capability mementos.
- [x] 5.3 Add SDK command helper builders for all `rendering.*` commands; helpers must only build canonical traced service calls and must never construct rendering clients, access credentials, fetch remote resources, execute scripts, read private assets/fonts directly, render pixels directly, export raw artifacts, or bypass policy.
- [x] 5.4 Extend WASM/app ABI descriptors so applications can discover rendering commands, declare permissions, receive unavailable diagnostics, and submit typed service calls through the canonical execution path.
- [x] 5.5 Add generic app-facing examples for provider inspection, source import/open, template inspection, scene graph inspection, asset/font validation, render planning/request, frame planning/request, animation planning/request, preview planning/request, export planning/request, job inspection, cancellation, and artifact handles.
- [x] 5.6 Add unavailable-provider, missing-source-permission, template-redacted, scene-redacted, format-unsupported, asset-denied, font-denied, script-denied, network-denied, shader-denied, GPU-unavailable, render-approval, export-denied, provider-quota, job-timeout, cancellation, and artifact-denied examples that demonstrate diagnostics without provider names, credentials, private assets, licensed fonts, raw templates, raw scripts, raw pixels, raw vector output, or workflow-specific conventions.

## 6. Trace, Audit, Replay, Security, And Gates

- [x] 6.1 Emit sanitized declaration, admission, provider-inspection, source import/open, template inspection, scene graph inspection, asset validation, render plan/request, frame plan/request, animation plan/request, preview plan/request, export plan/request, job inspection, cancellation, artifact-handle, policy, entitlement, resource, approval, health, snapshot, unavailable, and failure events.
- [x] 6.2 Ensure traces, audits, snapshots, SDK diagnostics, and examples exclude raw credentials, raw templates, raw scripts, private assets, licensed fonts, raw scene graphs, raw pixels, raw vector outputs, raw provider payloads, manifests, package bytes, private keys, signatures, and unbounded rendering data.
- [x] 6.3 Add replay tests proving every `rendering.*` command is trace-addressable through the canonical service path and that snapshots contain enough bounded metadata for recovery diagnostics.
- [x] 6.4 Add dependency gates proving kernel, SDK, shells, and generic application framework do not import concrete Skia, Cairo, Canvas, WebGPU, ImageMagick, librsvg, Lottie/Skottie, Headless Chrome, font, storage, moderation, credential-manager, artifact-provider, or export adapters.
- [x] 6.5 Add no-direct-provider-call gates proving SDK helpers, WASM ABI handlers, app admission, web, CLI, and frontend paths route through descriptor-owned service commands.
- [x] 6.6 Add boundary tests proving optional provider absence returns structured unavailable diagnostics and never crashes, hangs, silently falls back, reads private templates/assets/fonts, fetches remote resources, executes scripts, renders pixels, exports artifacts, publishes outputs, contacts providers, or fakes success.
- [x] 6.7 Run `openspec validate add-pack-media-rendering --strict`, targeted cargo tests, boundary gates, file-size gates, and audit replay checks before marking implementation complete.

## 7. Developer Documentation

- [x] 7.1 Create `docs/developer-packs/media/rendering.md` with purpose, capability model, manifest declaration, required versus optional behavior, permissions, provider scopes, source handles, template metadata, scene graph summaries, viewports, surfaces, asset handles, font references, render plans, frame plans, animation plans, preview plans, export plans, job status, artifacts, unavailable diagnostics, provider replacement, and operational limits.
- [x] 7.2 Document all command DTOs and result DTOs with field-level explanations, idempotency semantics, redaction behavior, pagination/asynchronous job behavior, timeout/cancellation behavior, plan/request behavior, approval behavior, artifact retention behavior, source version preconditions, format/surface/engine compatibility, script/network policy, asset/font validation, deterministic preview, GPU limits, export constraints, and structured error codes.
- [x] 7.3 Document supplier/API mapping: Skia, Cairo, Canvas 2D, WebGPU, ImageMagick, librsvg, Lottie/Skottie, Headless Chrome, font, asset, storage, and export concepts mapped to Macaca abstractions, including what is intentionally not exposed as OS semantics.
- [x] 7.4 Add generic examples for provider inspection, source import/open, template inspection, scene graph inspection, asset/font validation, render, frame, animation, preview, export, job status, cancellation, artifact handles, and unavailable diagnostics using synthetic rendering data only.
- [x] 7.5 Add conformance checklist and test guidance for provider authors: descriptor completeness, source/template/scene/job/artifact scope validation, format/surface support, asset/font validation, script/network enforcement, GPU/shader validation, render validation, animation validation, preview determinism, export validation, artifact redaction, resource bounds, policy hooks, trace/audit events, unavailable behavior, snapshot/replay, and redaction.
- [x] 7.6 Cross-link the guide from SDK discovery metadata and the industrial pack catalog index before marking `add-pack-media-rendering` complete.
