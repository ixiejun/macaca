## 1. End-To-End Closure

- [x] 1.1 Add OS-code proposal evaluation to the autonomy evolution service command surface.
- [x] 1.2 Add a runtime-host live execution bridge that runs live tick, target adapter dispatch, and audit replay in one traced command.
- [x] 1.3 Add tests proving Skill target dispatch, OS-code proposal dispatch, fail-closed unsupported targets, and replayed audit evidence.
- [x] 1.4 Validate OpenSpec, run targeted Rust tests, run diff checks, and commit the complete closure.
