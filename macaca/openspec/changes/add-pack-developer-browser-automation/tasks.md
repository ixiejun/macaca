## 1. Supplier API Research And Scope

- [x] 1.1 Re-read architecture governance, microkernel boundaries, serviceization allowlist, design patterns, OpenSpec rules, and the industrial catalog umbrella proposal before implementation.
- [x] 1.2 Study Playwright Browser, BrowserContext, Page, Locator, tracing, screenshot, download, network event, and action auto-waiting APIs.
- [x] 1.3 Study Chrome DevTools Protocol Page, Runtime, DOM, Network, Input, Target, Browser, and tracing/debugging domains.
- [x] 1.4 Study W3C WebDriver BiDi browsing context, script, input, network, log, storage, and event subscription modules.
- [x] 1.5 Study Selenium WebDriver local/remote session, navigation, element interaction, window/session lifecycle, and W3C WebDriver behavior.
- [x] 1.6 Produce a supplier capability comparison memo mapping Playwright, CDP, WebDriver BiDi, and Selenium concepts into Macaca provider-neutral browser automation DTOs and commands.
- [x] 1.7 Define explicit non-goals for concrete Playwright, Puppeteer, Selenium, CDP, WebDriver, browser engine, remote grid, profile store, extension provider, website-specific workflow, raw provider pass-through, and provider-specific routing.
- [x] 1.8 Record GitNexus CRITICAL/HIGH findings as memo only before implementation commits.

## 2. Contract, Descriptor, And DTOs

- [x] 2.1 Define `pack.developer.browser.automation.v1` descriptor metadata: pack id, family, lifecycle, stability, provider/browser support, context support, page support, frame support, locator support, action support, evaluation support, screenshot support, accessibility support, download/upload support, event/network/tracing support, storage support, auth modes, command schemas, permission scopes, policy templates, resource budgets, approval requirements, data-governance class, SDK metadata, documentation link, compatibility, and diagnostics.
- [x] 2.2 Define provider-neutral DTOs for `BrowserAutomationScope`, `BrowserProviderCapability`, `BrowserContextProfile`, `BrowserPage`, `BrowserFrame`, `BrowserLocator`, `BrowserNavigationPlan`, `BrowserActionPlan`, `BrowserEvaluationPlan`, `BrowserWaitCondition`, `BrowserArtifactHandle`, `BrowserNetworkEvent`, `BrowserConsoleEvent`, `BrowserDialogEvent`, `BrowserTraceEvent`, `BrowserStorageHandle`, and `BrowserSessionSnapshot`.
- [x] 2.3 Define typed command/result DTOs for `browser.inspect_provider`, `browser.plan_context`, `browser.open_context_request`, `browser.open_page`, `browser.navigate`, `browser.wait_for`, `browser.inspect_dom`, `browser.resolve_locator`, `browser.plan_action`, `browser.action_request`, `browser.plan_evaluate`, `browser.evaluate_request`, `browser.capture_screenshot`, `browser.capture_accessibility`, `browser.manage_download`, `browser.manage_upload`, `browser.inspect_events`, `browser.manage_storage_state`, `browser.close_page`, and `browser.close_context`.
- [x] 2.4 Define typed success, streaming, paged, partial, denied, unavailable, unsupported, conflict, stale-handle, not-found, ambiguous-locator, navigation-failed, actionability-failed, script-denied, artifact-denied, storage-denied, quota, timeout, cancellation, approval-required, and failure DTOs.
- [x] 2.5 Define stable descriptor hashing, provider capability hashing, context profile hashing, page/frame freshness hashing, locator compatibility hashing, navigation plan hashing, action plan hashing, evaluation plan hashing, artifact handle hashing, event cursor hashing, storage handle hashing, and redaction metadata.
- [x] 2.6 Add descriptor and DTO compatibility tests for valid descriptors, rejected invalid descriptors, stable hashes, schema evolution, context profiles, pages, frames, locators, navigation/action/evaluation plans, wait conditions, artifacts, events, storage handles, snapshots, redaction profiles, and serde compatibility.

## 3. Admission, Permission, Policy, Resource, Entitlement, And Approval

- [x] 3.1 Implement manifest declaration validation for required and optional `pack.developer.browser.automation.v1` declarations.
- [x] 3.2 Implement permission validation for `browser.provider.inspect`, `browser.context.open`, `browser.context.close`, `browser.page.open`, `browser.page.close`, `browser.navigate`, `browser.wait`, `browser.dom.inspect`, `browser.locator.resolve`, `browser.action.perform`, `browser.evaluate`, `browser.screenshot`, `browser.accessibility.inspect`, `browser.download.manage`, `browser.upload.manage`, `browser.events.inspect`, and `browser.storage.manage`.
- [ ] 3.3 Implement provider/context/page/frame/origin/artifact/storage scope checks for declared origins, authenticated pages, private contexts, local file handles, downloads, uploads, storage handles, denied scopes, and stale handles.
- [ ] 3.4 Implement policy checks for context isolation, origin allowlist, credential/storage policy, navigation policy, wait policy, locator strictness, actionability, side-effect classification, script sandbox, download/upload policy, screenshot redaction, DOM/accessibility redaction, network event redaction, storage export/import, trace retention, and output bounds.
- [ ] 3.5 Implement resource reservation for context count, page count, frame count, navigation timeout, action timeout, evaluation timeout, event count, screenshot size, artifact size, download/upload size, storage size, network bytes, memory, CPU, provider quota, and retained snapshots.
- [ ] 3.6 Implement entitlement checks and structured unavailable/denied diagnostics for missing provider, disabled pack, missing browser engine, unsupported context mode, unsupported locator/action/evaluation/screenshot/download/upload/storage/tracing support, disabled network, missing entitlement, provider quota, and host resource denial.
- [ ] 3.7 Implement approval behavior for authenticated pages, credential/storage access, cross-origin navigation, form submission, payment/financial or identity actions, downloads/uploads, local-file access, script evaluation, destructive actions, external side effects, and screenshot/export of sensitive pages.
- [ ] 3.8 Add tests proving denied, validation, quota, unavailable, conflict, stale-handle, not-found, ambiguous-locator, navigation-failed, actionability-failed, script-denied, artifact-denied, storage-denied, unsupported, timeout, cancellation, and approval-required paths do not call concrete providers, navigate, click, type, evaluate, screenshot, upload, download, read cookies, access local files, contact networks, or expose raw artifacts.

## 4. Service Provider And Runtime Integration

- [ ] 4.1 Implement or bind the browser automation service provider behind the service runtime; do not construct browser providers from SDK, shell, kernel, or application code.
- [x] 4.2 Add a deterministic unavailable provider that returns typed unavailable/unsupported diagnostics and complete discovery metadata.
- [ ] 4.3 Add mock provider support for provider inspection, context planning/request, page open/close, navigation, waits, DOM inspection, locator resolution, action planning/request, evaluation planning/request, screenshots, accessibility snapshots, downloads/uploads, event inspection, storage state, context close, health, and provider capability inspection.
- [ ] 4.4 Add lifecycle, health, snapshot, shutdown, timeout, cancellation, bounded artifact retention, event cursors, stale-handle diagnostics, actionability diagnostics, navigation diagnostics, and rate-limit diagnostics.
- [ ] 4.5 Add Strategy implementations for provider adapters, context isolation, origin policy, locator resolution, actionability validation, wait strategy, evaluation sandbox, artifact redaction, download/upload handling, event redaction, storage policy, trace retention, and unavailable behavior.
- [ ] 4.6 Add side-effect safety support for idempotency keys, provider state validation, page/frame freshness, resource reservation, approval state, action/evaluation plan handles, artifact retention, storage handle lifecycle, and non-mutating plan commands.
- [ ] 4.7 Add provider capability reporting for available, degraded, preview, unavailable, unsupported, retired, engine-limited, context-limited, page-limited, locator-limited, action-limited, evaluation-limited, artifact-limited, network-limited, storage-limited, tracing-limited, remote-grid-limited, and quota-limited states.

## 5. SDK, WASM ABI, Application Framework, And Examples

- [x] 5.1 Extend SDK discovery for `pack.developer.browser.automation.v1` with command schemas, provider/browser support, context/page/frame support, locator/action support, evaluation support, screenshot/accessibility support, download/upload support, event/network/tracing support, storage support, examples, availability, diagnostics, documentation link, provider class, capability hash, compatibility, and redaction profile.
- [x] 5.2 Extend application admission so required declarations block readiness when unavailable and optional declarations degrade explicitly with effective capability mementos.
- [x] 5.3 Add SDK command helper builders for all `browser.*` commands; helpers must only build canonical traced service calls and must never construct browser clients, access credentials, call browser APIs, navigate, click, type, evaluate, screenshot, upload, download, read cookies, read raw artifacts, or bypass policy.
- [ ] 5.4 Extend WASM/app ABI descriptors so applications can discover browser automation commands, declare permissions, receive unavailable diagnostics, and submit typed service calls through the canonical execution path.
- [x] 5.5 Add generic app-facing examples for provider inspection, context planning/request, page open, navigation, waits, DOM inspection, locator resolution, action planning/request, evaluation planning/request, screenshots, downloads/uploads, event inspection, storage handles, and cleanup.
- [x] 5.6 Add unavailable-provider, missing-origin-permission, unsupported-browser, ambiguous-locator, actionability-failed, script-denied, screenshot-redacted, download-denied, upload-denied, storage-approval, navigation-timeout, provider-quota, network-denied, and artifact-retention examples that demonstrate diagnostics without provider names, credentials, cookies, private DOM, screenshots, downloads, network payloads, or website-specific conventions.

## 6. Trace, Audit, Replay, Security, And Gates

- [ ] 6.1 Emit sanitized declaration, admission, provider-inspection, context-plan, context-open, page-open, navigation, wait, DOM-inspection, locator-resolution, action-plan, action-request, evaluation-plan, evaluation-request, screenshot, accessibility, download/upload, event-inspection, storage-management, page-close, context-close, policy, entitlement, resource, approval, health, snapshot, unavailable, and failure events.
- [ ] 6.2 Ensure traces, audits, snapshots, SDK diagnostics, and examples exclude raw cookies, credentials, storage values, local file contents, screenshots, DOM dumps, downloads, uploads, network payloads, provider payloads, prompts, manifests, package bytes, private keys, signatures, and unbounded browser logs.
- [ ] 6.3 Add replay tests proving every `browser.*` command is trace-addressable through the canonical service path and that snapshots contain enough bounded metadata for recovery diagnostics.
- [ ] 6.4 Add dependency gates proving kernel, SDK, shells, and generic application framework do not import concrete Playwright, Puppeteer, Selenium, CDP, WebDriver, browser engine, remote-grid, profile-store, credential-manager, filesystem-provider, network-provider, or website-specific adapters.
- [ ] 6.5 Add no-direct-provider-call gates proving SDK helpers, WASM ABI handlers, app admission, web, CLI, and frontend paths route through descriptor-owned service commands.
- [ ] 6.6 Add boundary tests proving optional provider absence returns structured unavailable diagnostics and never crashes, hangs, silently falls back, creates browser contexts, navigates, clicks, types, evaluates scripts, screenshots, uploads/downloads, reads cookies, contacts networks, reads local files, emits raw artifacts, or fakes success.
- [ ] 6.7 Run `openspec validate add-pack-developer-browser-automation --strict`, targeted cargo tests, boundary gates, file-size gates, and audit replay checks before marking implementation complete.

## 7. Developer Documentation

- [x] 7.1 Create `docs/developer-packs/developer/browser-automation.md` with purpose, capability model, manifest declaration, required versus optional behavior, permissions, provider scopes, browser contexts, pages, frames, locators, navigation, waits, DOM inspection, actions, evaluation, screenshots, accessibility, downloads, uploads, events, storage state, traces, cleanup, unavailable diagnostics, provider replacement, and operational limits.
- [x] 7.2 Document all command DTOs and result DTOs with field-level explanations, idempotency semantics, redaction behavior, artifact/pagination/streaming behavior, timeout/cancellation behavior, plan/request behavior, approval behavior, storage/download retention behavior, and structured error codes.
- [x] 7.3 Document supplier/API mapping: Playwright, Chrome DevTools Protocol, W3C WebDriver BiDi, and Selenium WebDriver concepts mapped to Macaca abstractions, including what is intentionally not exposed as OS semantics.
- [x] 7.4 Add generic examples for provider inspection, context creation, navigation, locator/action, evaluation, screenshots, downloads/uploads, event inspection, storage handles, cleanup, and unavailable diagnostics using synthetic browser data only.
- [x] 7.5 Add conformance checklist and test guidance for provider authors: descriptor completeness, context isolation, origin policy, locator/action validation, evaluation sandbox, artifact redaction, download/upload safety, storage policy, event redaction, resource bounds, policy hooks, trace/audit events, unavailable behavior, snapshot/replay, and redaction.
- [x] 7.6 Cross-link the guide from SDK discovery metadata and the industrial pack catalog index before marking `add-pack-developer-browser-automation` complete.
