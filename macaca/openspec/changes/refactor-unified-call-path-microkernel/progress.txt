# Progress — refactor-unified-call-path-microkernel

## Iteration 1 (2026-06-07)
- Started Ralph loop after proposal approval.
- Build fix: removed corrupted `macaca/target` directory; `cargo check -p macaca-kernel` green.
- **P0 complete**: escape hatch tokens 1.1.1–1.1.7, baseline.md, impact-memo.md, allowlist doc sync, VC-escape + VC-gate green.
- **P1 partial**: `ServiceClientAgentExecutionAdapter` + `AgentExecutionDispatch` in macaca-agent; `ServiceRuntimeAgentExecutionDispatch` + `kernel_service_client_compat_from_agent_execution_service` in runtime-host (2 unit tests pass).
- **P2 partial**: `payment_policy` module internalized in kernel lib.rs (subagent).
- **Blocker**: macaca-web bootstrap creates kernel (LegacyAgentExecutionAdapter) before `service_runtime` + agent execution provider; needs reorder to wire `kernel_service_client_compat_from_agent_execution_service`.
- Next: 0.3 audit replay baseline, web bootstrap reorder, P1.2–P1.6, P2 eviction.

## Iteration 2 (2026-06-07)
- **P1 kernel wiring**: `SwappableAgentExecutionPort` (macaca-agent); `Kernel::replace_execution_port`; `wire_kernel_to_agent_execution_service` (runtime-host); web calls hot-swap after agent execution provider starts.
- Tests: `macaca-runtime-host` agent_execution_dispatch 3/3; `macaca-kernel` execute_agent; `cargo check -p macaca-web` green.
- Remaining P1: WebAgentExecutionBackend 下沉 runtime-host; 删协调补丁（需 0.3 audit replay 门）; framework_toolkit/registry 直读迁移.

## Iteration 3 (2026-06-07)
- **P1.5 MCP service path**: `framework_runner` uses `probe_mcp_capability_inputs_via_client`; `chat_orchestrator` session teardown uses `mcp_client.cleanup` (no direct `mcp_runtime.cleanup_session`); new unit test `probe_mcp_capability_inputs_via_client_maps_service_status_views`.
- **P2 prep (payment_policy)**: implementation moved to `macaca-proto`; kernel re-export only; runtime-host `payment_policy` module + `payment_service_provider` imports updated.
- **Verified**: `cargo test -p macaca-web capability_catalog` 11/11; `cargo test -p macaca-integration-tests serviceization_escape_hatches` pass; `cargo check -p macaca-web` green.
- **Remaining mcp_runtime shell reads**: `lib.rs` bootstrap (feeds MCP service provider), `state.rs` deprecated field, dead `skill_mcp::register_skill_backed_mcp_tools`.
- **Remaining P1 blockers**: WebAgentExecutionBackend still owns framework loop (~898 lines); kernel `ApplicationExecutor` still used by loop_manager/framework_runner/orchestration_tools; coordination patches frozen not removed (0.3 audit replay gate).

## Iteration 4 (2026-06-07)
- **P1.2 orchestration sink (partial)**: new `macaca-runtime-host/src/agent_execution_orchestration.rs` — context build, prompt composition, heartbeat guards, execution-policy Strategy, execution-control scope, failed result helper; Web backend delegates (~300 lines semantic logic moved).
- **P1.3 inventory**: `executor-consumer-inventory.md` (47 rows; live executor coupling only in macaca-web).
- **Verified**: `cargo test -p macaca-runtime-host --lib chat_main_thread` pass; `cargo test -p macaca-web agent_execution_backend` 24/24 pass.
- **Next**: 2.2.2–2.2.4 full runtime-host backend (FrameworkRunner/agent.reply out of web); 2.3.2–2.3.5 delegate_task → service.agent_execution; 0.3 audit replay baseline before 2.6 patch removal.

## Iteration 5 (2026-06-07)
- **P1.2 composed backend**: `ComposedAgentExecutionBackend` (runtime-host Template Method) + `agent_execution_ports` (Host/Framework ports) + `web_agent_execution_adapters` (Web Adapter injection). Web no longer owns `execute()` — deleted monolithic `WebAgentExecutionBackend`.
- **Heartbeat shell** moved to `agent_execution_orchestration::execute_exact_heartbeat_shell`.
- **Verified**: `cargo test -p macaca-web agent_execution_backend` 24/24; `cargo check -p macaca-runtime-host -p macaca-web` green.
- **Remaining 2.2.3**: `WebFrameworkRuntimeAgentPort` still calls `FrameworkRunner` (framework construction stays in web until runtime/framework service exists).
- **Remaining 2.3**: kernel executor queue/fork still used by orchestration_tools; worker path already routes via `WebAgentRunner` → `service.agent_execution`.

## Iteration 6 (2026-06-07)
- **P1.3 delegate_task service path (2.3.3)**: `ServiceDelegatedTaskDispatcher` (`macaca-web/delegated_task_dispatcher.rs`) routes `delegate_task` tool through `service.agent_execution` (`TaskWorker` intent) instead of `ApplicationExecutor::delegate_task` worker channel.
- **Kernel bridge**: `ExecutionQueue::admit_running_task`, `ApplicationExecutor::begin_service_backed_delegation`, `complete_service_backed_delegation` (admission + lifecycle events + fork resume).
- **Provider-neutral delegator label**: replaced hardcoded `"coordinator"` with `GENERIC_DELEGATOR_AGENT = "delegator"`.
- **Verified**: `cargo check -p macaca-kernel -p macaca-web`; `cargo test -p macaca-kernel --lib test_admit_running_task_respects_concurrency_limit`; `cargo test -p macaca-web delegated_task_dispatcher` 2/2.
- **Remaining P1**: 2.2.3 FrameworkRunner sink; 2.3.2 execution_control Fork-Join; 2.6 coordination patch removal (0.3 audit replay gate); `suppress_executor_lifecycle` still written by delegate/agent_runner/loop_manager paths.

## Iteration 7 (2026-06-07)
- **P1.3 Fork-Join execution_control (2.3.2)**: `ExecutionControlForkJoinCoordinator` in runtime-host (Facade + Command); pause register + resume deliver via `service.execution_control`.
- **Web wiring**: `orchestration_tools` uses coordinator for fork create/suspend + `register_parent_fork_wait`; `fork_join_shell_adapter` + `hook_consumer` call `deliver_parent_fork_resume` before legacy channel adapter; `fork_to_session` shared map registered on delegate_task.
- **Task 0.3**: `audit-replay-baseline.md` — YAML 3 / WASM 3 pre-convergence chains documented.
- **Verified**: pending `cargo check -p macaca-runtime-host -p macaca-web` + unit tests this iteration.
- **Remaining P1**: 2.2.3 FrameworkRunner sink; 2.3.4 loop_manager consumes execution_control events; 2.6 coordination patch removal (gate: audit replay → 1 chain); kernel ForkManager eviction (P3).

## Iteration 8 (2026-06-07)
- **P1.3 goal-lifecycle execution_control (2.3.4)**: `ExecutionControlGoalLifecycleCoordinator` (runtime-host Facade + Command); `goal_lifecycle_shell_adapter` (web Adapter).
- **PlanLoop wiring**: `GoalCompleted` → `deliver_goal_resume_and_notify_parent` (execution_control resume + legacy channel compat).
- **create_goal wiring**: `framework_toolkit` registers `register_goal_wait_via_execution_control` when goal_to_session mapping is recorded.
- **P1.3 integration contracts (2.3.5)**: `unified_delegation_path_tests.rs` — fork-join delegate_task + goal-task + worker_loop service path assertions (5/5).
- **Verified**: `cargo test -p macaca-runtime-host --lib execution_control_goal_lifecycle` 5/5; `cargo test -p macaca-web unified_delegation_path` 5/5; `cargo check -p macaca-runtime-host -p macaca-web` green.
- **Remaining P1**: 2.2.3 FrameworkRunner sink; 2.2.5 agent execution integration tests; 2.4 YAML workflow merge; 2.6 coordination patch removal (gate: audit replay → 1 chain).

## Iteration 9 (2026-06-07)
- **P1.2 framework runtime agent port (2.2.3)**: `ServiceBackedFrameworkRuntimeAgentPort` (runtime-host Template Method + Adapter); `FrameworkAgentConstructionPort` trait; `WebFrameworkAgentConstructionPort` shell adapter delegates to `FrameworkRunner` compat seam.
- **Removed**: `WebFrameworkRuntimeAgentPort` — web no longer implements `FrameworkRuntimeAgentPort` directly.
- **Contract test**: `framework_runtime_agent_port_owned_by_runtime_host_service` in `unified_delegation_path_tests.rs` (6/6 total).
- **Verified**: `cargo check -p macaca-runtime-host -p macaca-web`; `cargo test -p macaca-runtime-host --lib framework_runtime_agent_service` 2/2; `cargo test -p macaca-web unified_delegation_path` 6/6.
- **Remaining P1**: 2.2.5 agent execution integration tests; 2.4 YAML workflow merge; 2.6 coordination patch removal (gate: audit replay → 1 chain); `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 10 (2026-06-07)
- **P1.2 agent execution integration tests (2.2.5)**: `unified_agent_execution_provider_tests.rs` (web contract 7/7 + runtime-host provider integration 3/3).
- **Coverage**: YAML chat (`chat_orchestrator`), YAML workflow (`agent_runner`), WASM delegate (`wasm_orchestration_backend`) → single `ComposedAgentExecutionBackend` via `service.agent_execution`; shared `trace.system_service.agent_execution.v1` + `agent.execute` command.
- **Verified**: `cargo test -p macaca-web unified_agent_execution_provider` 7/7; `cargo test -p macaca-runtime-host unified_agent_execution_provider` 3/3.
- **Remaining P1**: 2.4 YAML workflow merge; 2.6 coordination patch removal (gate: audit replay → 1 chain); `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 11 (2026-06-07)
- **P1.4 YAML workflow merge (2.4)**: `workflow-execution-inventory.md` documents prompt-only `macaca-app/workflow.rs` vs `WebAgentRunner` execution gap.
- **Application ABI unification**: `WebAgentRunner` now calls `service.application` / `application.agent.delegate` with `execution_intent=yaml_workflow_step` metadata instead of direct `service.agent_execution`.
- **Shared bridge**: `application_agent_delegate_bridge.rs` (Bridge + Strategy) centralizes delegate→execution dispatch for YAML and WASM; `wasm_orchestration_backend` refactored to use it.
- **Proto**: `AgentExecutionIntent::metadata_value/from_delegate_metadata`; `ApplicationAgentDelegateCommand::into_service_command`.
- **Contract tests**: `unified_workflow_application_abi_tests.rs` 6/6; updated `unified_agent_execution_provider_tests` (7/7); `unified_delegation_path` 6/6 unchanged.
- **Verified**: `cargo check -p macaca-proto -p macaca-runtime-host -p macaca-web`; `cargo test -p macaca-web unified_` 19/19.
- **Remaining P1**: 2.6 coordination patch removal (gate: audit replay → 1 chain); 2.7 P1 exit validation; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 12 (2026-06-07)
- **P1.6 coordination patch removal (2.6)**: deleted `authoritative/non_authoritative/legacy_unmarked` terminal reconciliation in `application_execution_hosted.rs`; removed `graph_owner` compat metadata from `host_import_bridge.rs`; removed `suppress_executor_lifecycle` / `legacy_chat_main_thread_goal_pause` OS branches.
- **Manifest-driven execution control**: `AppManifest.execution_control` + yaml projection; `fullstack-autodev/app.yaml` declares `create_goal` pause + `goal_lifecycle` resume; web adapter resolves policy via `application_client.metadata()` (not `state.registry`).
- **Proto projection**: `ApplicationMetadataView.execution_control` exposed through Application Service metadata path.
- **Contract tests**: updated `agent_execution_backend/tests.rs` for manifest-required overrides + shell adapter allowlist; `serviceization_escape_hatches` green.
- **Verified**: `cargo check -p macaca-runtime-host -p macaca-web -p macaca-app`; `cargo test -p macaca-web agent_execution_backend` 28/28; `cargo test -p macaca-integration-tests serviceization_escape_hatches_reject` pass.
- **Remaining P1**: 2.7 P1 exit validation (audit replay single chain + route-c regression); `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 13 (2026-06-07)
- **P1 exit validation (2.7)**: `unified_audit_replay_convergence_tests.rs` (web 6/6 + runtime-host 3/3); post-convergence report in `audit-replay-baseline.md` (YAML/WASM each 1 chain, down from 3).
- **Regression fixes (2.7.2)**: `execution_control` YAML variant tags (`!ToolCallBarrier` / `!GoalLifecycle`); `ExecutionControlPolicy` serde defaults in `macaca-proto`; `AppManifest.execution_control` test fixtures; `fullstack-autodev/app.yaml` + missing `personas/frontend/` fixture; `.gitignore` narrowed to `/frontend/` so example-app persona dirs track.
- **VC-escape (2.7.3)**: `serviceization_escape_hatches_reject_new_production_references` violations=0.
- **Verified**: `cargo test -p macaca-web unified_audit_replay_convergence` 6/6; `cargo test -p macaca-runtime-host --lib unified_audit_replay_convergence` 3/3; `cargo test -p macaca-integration-tests --test route_c_baseline` 4/4; `cargo test -p macaca-integration-tests --test fullstack_autodev` 10/10 (4 ignored); `cargo test -p macaca-app` 131/131.
- **P1 complete** (tasks 2.1–2.7). **Next**: P2 kernel purification (3.1.2+); `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 14 (2026-06-07)
- **P2.1 payment/A2A kernel eviction (3.1.2–3.1.4)**: moved `local_simulated_terms` factory to `macaca-proto/a2a.rs`; updated web bootstrap, runtime-host bootstrap, SDK payment client tests, and payment provider integration tests to use proto contract.
- **Kernel cleanup (3.1.3)**: deleted `kernel/a2a.rs`, `a2a_event.rs`, `payment_policy.rs`; removed deprecated `KernelPaymentStore*` from `persistence.rs`; dropped `lib.rs` A2A/payment_policy re-exports.
- **Service-path tests (3.1.4)**: `payment_service_provider.rs` 5/5 (quote/settle/trace/policy-denied/invalid-transition); `macaca-sdk` `UnavailableSystemPaymentClient` 3/3; VC-escape `serviceization_escape_hatches_reject_new_production_references` violations=0.
- **Verified**: `cargo check -p macaca-kernel -p macaca-proto -p macaca-runtime-host -p macaca-sdk -p macaca-web`; payment + escape hatch tests green.
- **Next**: P2 §3.2 web3 kernel eviction; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 15 (2026-06-07)
- **P2.2 web3 kernel eviction (3.2.1–3.2.4)**: confirmed zero production consumers of `macaca_kernel::web3`; canonical path is `Web3SystemServiceProvider` + `SystemWeb3Client` via Route C bootstrap.
- **Kernel cleanup (3.2.3)**: deleted `kernel/web3.rs`, `web3_event.rs`, `web3_tests.rs`; removed `lib.rs` Web3 facade/adapter re-exports.
- **Service-path tests (3.2.4)**: `web3_service_provider.rs` 6/6 (unavailable snapshot/signing/health, mock sign/tx/query/wallets, trace-required); `macaca-sdk` `web3_client` 2/2; VC-escape `serviceization_escape_hatches_reject_new_production_references` violations=0.
- **Verified**: `cargo check -p macaca-kernel -p macaca-runtime-host -p macaca-sdk -p macaca-web`; web3 + escape hatch tests green.
- **Next**: P2 §3.3 EVM kernel eviction; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 16 (2026-06-07)
- **P2.3 EVM kernel eviction (3.3.1–3.3.4)**: confirmed zero production consumers of `macaca_kernel::evm`; canonical path is `EvmSystemServiceProvider` + `SystemEvmClient` via Route C bootstrap.
- **Kernel cleanup (3.3.3)**: deleted `kernel/evm.rs`, `evm_adapter.rs`, `evm_event.rs`, `evm_tests.rs`; removed `lib.rs` EVM facade/adapter re-exports.
- **SDK cleanup**: deleted deprecated `macaca-sdk/src/evm.rs` (`MacacaEvmSdk`); consumers use `SystemEvmClient` / `ServiceBackedEvmClient`.
- **Service-path tests (3.3.4)**: `evm_service_provider.rs` 5/5 (unavailable snapshot/deploy/health, mock deploy/call/read/gas/receipt/subscribe, trace-required); `macaca-sdk` `evm_client` 2/2; VC-escape `serviceization_escape_hatches_reject_new_production_references` violations=0.
- **Verified**: `cargo check -p macaca-kernel -p macaca-runtime-host -p macaca-sdk -p macaca-web`; evm + escape hatch tests green.
- **Next**: P2 §3.4 kernel executor eviction; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 16 merge note (2026-06-07)
- Merged `origin/main` (PR #5 P1 coordination-patch removal at `53dc7f6`); no conflicting intents — main simply lacked P2.3/P2.4 iterations.

## Iteration 17 (2026-06-07)
- **P2.4 kernel executor eviction (3.4.1–3.4.4)**: physically moved `kernel/src/executor/` (9 files) to `macaca-runtime-host/src/executor/`; removed `pub mod executor` and all re-exports from `macaca-kernel/src/lib.rs`.
- **Import migration**: ~17 macaca-web + runtime-host consumers updated from `macaca_kernel::executor::*` to `macaca_runtime_host::{...}` / `crate::executor::*`; production `macaca_kernel::executor` references = 0.
- **Runtime-host wiring**: added `pub mod executor` + root re-exports in `lib.rs`; `futures` workspace dep for executor queue; executor internals use `macaca_kernel::KernelPersistencePort` / `logging` ports only (Strangler Fig physical move).
- **Tests (3.4.3)**: `executor_runtime_eviction.rs` 5/5 (registry, event factory, fork manager + kernel port, static kernel contract, TaskStatus); VC-escape `serviceization_escape_hatches_reject_new_production_references` violations=0; escape hatch allowlist paths updated to `runtime-host/.../executor/`.
- **Verified**: `cargo check -p macaca-kernel -p macaca-runtime-host -p macaca-web`; executor eviction + escape hatch tests green.
- **Next**: P2 §3.6 kernel dependency allowlist cleanup; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 18 (2026-06-07)
- **P2.5 provider compat eviction (3.5.1–3.5.6)**: deleted `kernel/provider_compat.rs`; sole kernel construction is `KernelBuilder::from_execution_port` + `AgentExecutionPort`; removed `Kernel::new(config, llm, tools)` and `KernelServiceClientCompat` paths.
- **Runtime-host rename (3.5.5)**: `compat.rs` → `skill_mcp_mapping_registry.rs` (`SkillMcpMappingRegistry`, `default_skill_mcp_mapping_registry`, `McpServerFactory::with_bundled_mapping_registry`); TOML `[[compat]]` schema retained for operator overrides.
- **Consumers**: web bootstrap uses `UnavailableAgentExecutionPort` + hot-swap; runtime-host `kernel_execution_port_from_agent_execution_service`; integration tests use `LegacyAgentExecutionAdapter` fixtures.
- **Escape hatch**: removed deleted `provider_compat.rs` allowlist paths; narrowed `provider-compat-construction` to agent/app/sdk migration surfaces.
- **Verified**: `cargo check -p macaca-kernel -p macaca-runtime-host -p macaca-web` (no kernel deprecated warnings); kernel + skill mapping + escape hatch tests green.
- **GitNexus memo (non-blocking)**: `KernelProviderCompat`/`KernelServiceClientCompat` deletion — CRITICAL/HIGH blast radius recorded in `impact-memo.md` iteration 18.
- **Next**: P2 §3.6 kernel Cargo.toml dependency eviction; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 19 (2026-06-07)
- **P2.6 kernel boundary cleanup (3.6.1–3.6.6)**: `AgentExecutionPort` trait下沉 `macaca-proto`; `SwappableAgentExecutionPort`/`UnavailableAgentExecutionPort` 迁入 `macaca-kernel/src/execution_port.rs`; kernel registry manifest-only (`register_agent(manifest)`); 删除 `kernel/services.rs` 与生产依赖 `macaca-agent`。
- **Legacy migration**: `LegacyAgentSideRegistry` + `LegacyAgentExecutionAdapter` 保留于 `macaca-agent`; `register_legacy_kernel_agent` 置于 `macaca-sdk`; SDK `for_kernel_with_legacy` 双写 manifest + runtime agent。
- **Consumers**: kernel_lifecycle, live_fullstack_autodev, e2e_auto_programming, runtime-host dispatch tests, facade tests 已迁移。
- **Allowlist**: 删除 kernel→driver/gateway/skill 三行（Rust + `macaca-os-serviceization-allowlist.md`）；Route C gate 7 行（仅 web）。
- **GitNexus memo (non-blocking)**: iteration 19 HIGH blast radius on `AgentExecutionPort` + registry API — recorded in `impact-memo.md`.
- **Next**: P2 §3.7 persist→context 越界反转; P2 §3.8 退出验证; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 20 (2026-06-07)
- **P2.7 persist→context boundary inversion (3.7.1–3.7.3)**: extracted `LineageKind`/`SessionLineage`/`TranscriptSegment` to `macaca-proto/src/context_lineage.rs` (Shared Kernel DTO); `macaca-context::compaction` re-exports proto types; `macaca-persist` imports proto only; `macaca-context` moved to dev-dependency for cross-crate pruning contract test.
- **SessionLineageStore**: added provider-neutral `tracing` on save/load/segment paths; English module docs (Repository pattern).
- **P2.8 exit validation (3.8.1–3.8.3)**: `p2_microkernel_exit_validation.rs` static contracts (kernel lib purity, persist Cargo.toml, cargo tree); VC-gate 7 web rows green; `package_certification_keeps_web3_and_evm_optional_modules_unavailable_safe` green.
- **Verified**: `cargo test -p macaca-persist` 4/4; `cargo test -p macaca-context` 38/38; `cargo test -p macaca-proto` 153/153; `p2_microkernel_exit_validation` 3/3; VC-gate + escape hatch green.
- **GitNexus memo (non-blocking)**: iteration 20 MEDIUM on lineage DTO relocation — recorded in `impact-memo.md`.
- **Next**: P3 §4.1 AppState field removal; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 21 (2026-06-07)
- **P3.1 thin shell (4.1.1–4.1.3)**: removed deprecated `AppState` provider fields (`runtime`/`registry`/`llm`/`llm_router`/`memory_runtime`/`mcp_runtime`/`driver_*`); added `WebShellCompositionBundle` + shell adapters (`application_shell_adapter`, `llm_route_shell_adapter`, `mcp_shell_adapter`).
- **Consumer migration**: routes, loop_manager, framework_runner, framework_toolkit, chat_orchestrator, skill_mcp, app_ui_routes, skill_self_evolution_audit now call adapters or SDK clients — zero `state.registry`/`state.runtime` production reads outside adapters.
- **Escape hatch tighten**: `serviceization_escape_hatches` allowlist narrowed to bootstrap (`lib.rs`), composition bundle, adapters, and `framework_runner` (`state.llm_client` compat); violations=0.
- **Deferred 4.1.4**: `memory_runtime.rs` retained — bootstrap still wires `WebMemoryRuntime` into composition bundle and memory service provider.
- **Verified**: `cargo check -p macaca-web` green; `cargo test -p macaca-integration-tests serviceization_escape_hatches` violations=0.
- **GitNexus memo (non-blocking)**: `AppState` field removal — HIGH blast radius on routes/chat/framework paths recorded in `impact-memo.md` iteration 21.
- **Next**: P3 §4.2 session loop下沉; §4.3 巨型文件拆分; `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 22 (2026-06-07)
- **P3.2 session loop下沉 (4.2.1–4.2.2)**: added `ExecutionControlSessionLoopCoordinator` (runtime-host Facade + Command DTO) and `session_loop_shell_adapter` (web Adapter).
- **loop_manager wiring**: plan/worker register on spawn; all wake paths route through `wake_*_and_notify_local` (execution_control audit before legacy waker); worker success + review completion wakes migrated.
- **chat_orchestrator wiring**: `cleanup_app_state` records `shutdown_session_loops_via_execution_control` before local handle/waker removal.
- **Contract tests**: `unified_delegation_path_tests` session-loop assertion (6/6 total); `execution_control_session_loop` runtime-host unit tests 3/3.
- **Verified**: `cargo check -p macaca-web`; `cargo test -p macaca-runtime-host execution_control_session_loop`; `cargo test -p macaca-web unified_delegation_path`; `cargo test -p macaca-integration-tests serviceization_escape_hatches` violations=0.
- **GitNexus memo (non-blocking)**: iteration 22 HIGH on `loop_manager` wake/register paths + new coordinator — recorded in `impact-memo.md`.
- **Next**: P3 §4.3 巨型文件拆分 (`loop_manager.rs` ~2620 lines); `FrameworkRunner` body still in web (task 4.3.2).

## Iteration 23 (2026-06-07)
- **P3.3 loop_manager split (4.3.1)**: deleted monolithic `loop_manager.rs` (~2629 lines); extracted `loop_manager/` module tree (18 files, max 412 lines).
- **Design patterns**: Facade (`mod.rs` + `loop_orchestrator.rs`); Adapter (`execution_control_adapter`, `agent_execution_adapter`, `worker_execution_adapter`, `decomposition_adapter`, `sse_channel_adapter`); Strategy (`plan_event_consumer` dispatches per `PlanEvent` variant to dedicated handlers).
- **Plan event handlers**: `plan_loop_orchestrator` (startup only), `plan_event_consumer`, `plan_event_goal_ready`, `plan_event_goal_lifecycle`, `plan_event_handlers`.
- **Governance fixes**: `decomposition_adapter` removed hardcoded `"frontend"`/`"backend"` capability probes; fallback tasks use `TaskGraphOwner::ApplicationExecution` (not `TaskServiceCompatibility`).
- **Contract tests**: `unified_delegation_path_tests` + `agent_execution_backend/tests` source-scan via `contract_source::loop_manager_module_sources()`.
- **Verified**: `cargo check -p macaca-web`; `cargo test -p macaca-web loop_manager::tests` 16/16; `cargo test -p macaca-web unified_delegation_path` 7/7; `cargo test -p macaca-integration-tests serviceization_escape_hatches` violations=0.
- **GitNexus memo (non-blocking)**: iteration 23 HIGH on `ensure_plan_and_worker_loops` / `create_goal` / plan-event consumer split — recorded in `impact-memo.md`.
- **Next**: P3 §4.3.2 `framework_runner.rs` split; `FrameworkRunner` body still in web.

## Iteration 24 (2026-06-07)
- **P3.3 framework_runner split (4.3.2)**: deleted monolithic `framework_runner.rs` (~2484 lines); extracted `framework_runner/` module tree (23 files, max 480 lines).
- **Design patterns**: Facade (`mod.rs`); Adapter (`sse_emitter_adapter`, `channel_emitter_adapter`, `executor_emitter_adapter`, `execution_control_middleware`, `driver_trace_adapter`); Factory (`agent_factory`, `agent_factory_build`, `agent_factory_coordinator`).
- **Builders/context**: `traced_builders`, `runtime_agent_builders`, `coordinator_builder`, `context_prompt_builder`, `context_snapshot_builder`, `request_composition`, `agent_resolution`, `prompt_helpers`, `skill_policy`.
- **Contract tests**: `framework_runner::tests` via `contract_source::framework_runner_module_sources()`; `agent_execution_backend/tests` approved_files updated.
- **Escape hatch**: `build_mode.rs` + `sse_emitter_adapter.rs` on `hardcoded-agent-role` allowlist (DriverTraceRoute label + coordinator trace debt); `web-direct-runtime-field` prefix for `framework_runner/`.
- **Verified**: `cargo check -p macaca-web`; `cargo test -p macaca-web --lib framework_runner::tests` 13/13; `cargo test -p macaca-integration-tests serviceization_escape_hatches` violations=0.
- **GitNexus memo (non-blocking)**: iteration 24 CRITICAL on `FrameworkRunner` facade / `build_runtime_agent` split — recorded in `impact-memo.md`.
- **Next**: P3 §4.3.3 `chat_orchestrator.rs` split.

## Iteration 25 (2026-06-07)
- **P3.3 chat_orchestrator split (4.3.3)**: deleted monolithic `chat_orchestrator.rs` (~1695 lines); extracted `chat_orchestrator/` module tree (18 files, max ≤500 lines).
- **Design patterns**: Facade (`mod.rs`); Adapter (`sse_adapter`, `executor_event_adapter`, `session_persistence_adapter`, `application_service_adapter`, `wasm_dispatch_adapter`, `agent_activity_adapter`, `agent_execution_adapter`, `executor_adapter`); route paths (`route_chat_v2`, `wasm_chat_path`, `framework_chat_path`).
- **Contract tests**: `chat_orchestrator::tests` via `contract_source::chat_orchestrator_module_sources()`; unified_delegation_path, unified_agent_execution_provider, unified_audit_replay_convergence, agent_execution_backend updated.
- **Escape hatch**: `route_chat_v2.rs` on `hardcoded-agent-role` allowlist (entry-agent `"coordinator"` fallback debt); `chat_orchestrator/` prefix for pause/resume channel ownership.
- **Verified**: `cargo check -p macaca-web`; `cargo test -p macaca-web chat_orchestrator::tests`; contract test suites; `serviceization_escape_hatches` violations=0.
- **GitNexus memo (non-blocking)**: iteration 25 CRITICAL on `post_chat_v2` / `run_wasm_chat_fast_path` / `run_framework_chat_path` split — recorded in `impact-memo.md`.
- **Next**: P3 §4.3.4 `lib.rs` split.

## Iteration 26 (2026-06-07)
- **P3.3 lib.rs split (4.3.4)**: deleted monolithic `lib.rs` bootstrap body (~1417 lines); extracted `composition_bootstrap/` module tree (13 files, max 447 lines); thin `lib.rs` (110 lines).
- **Design patterns**: Facade (`mod.rs` + `serve.rs`); Composition Root (`bootstrap_ctx` carrier + phased `run` modules); route table remains in `bootstrap.rs` (`WebRuntimeFacade`).
- **Phase modules**: `config_and_kernel`, `application_discovery`, `tooling_and_persist`, `service_runtime_wiring`, `service_client_facades`, `app_state_assembly`, `post_bootstrap_hooks`, `bootstrap_path_helpers`.
- **Contract tests**: `contract_source::composition_bootstrap_module_sources()`; unified_agent_execution_provider, unified_audit_replay_convergence, agent_execution_backend updated.
- **Verified**: `cargo check -p macaca-web`; `cargo test -p macaca-web --lib` 250/250; `cargo test -p macaca-integration-tests serviceization_escape_hatches_reject_new_production_references` violations=0.
- **GitNexus memo (non-blocking)**: iteration 26 CRITICAL on `serve_web_server` / composition bootstrap split — recorded in `impact-memo.md`.
- **Next**: P3 §4.4 web 越界依赖 + allowlist 行。

## Iteration 27 (2026-06-07)
- **P3.4 web dependency decoupling (4.4.1–4.4.3, 4.5.2)**: removed 7 direct provider + kernel `Cargo.toml` edges from `macaca-web`.
- **Facade bridge**: `macaca-sdk/src/shell_provider_bridge.rs` re-exports driver/llm/memory/skill/task/tools/kernel crate aliases (Facade pattern).
- **Persist seam**: SDK cannot depend on `macaca-persist` (gate `application-execution-sdk-no-runtime-provider-construction`); `macaca-runtime-host::persist` alias added for shell bootstrap wiring.
- **Import migration**: all `macaca-web` sources use `macaca_sdk::{driver,llm,...}` and `macaca_runtime_host::persist` / direct `macaca_runtime_host` for host types.
- **Allowlist**: 7 web→provider rows cleared; `allowlist.rs` empty; `macaca-os-serviceization-allowlist.md` synced.
- **Deferred 4.4.4**: `macaca-runtime-host` direct dep retained (composition root); `app→sdk→runtime-host→app` cycle blocks SDK bridge for runtime-host.
- **Verified**: `cargo check -p macaca-web`; `cargo test -p macaca-web --lib` 250/250; `route_c_dependency_boundaries_reject_unallowlisted_forbidden_edges` pass; `cargo tree -p macaca-web --depth 1` no driver/llm/memory/persist/skill/task/tools/kernel.
- **GitNexus memo (non-blocking)**: iteration 27 CRITICAL on bulk import rewrite across macaca-web — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 filesize gate; P4 CLI decoupling; break app/sdk/runtime-host cycle for 4.4.4终态.

## Iteration 28 (2026-06-07)
- **P3.3 routes split (4.3.6)**: deleted monolithic `routes.rs` (2591 lines); extracted `routes/` module tree (16 files, max 438 lines).
- **Design patterns**: Facade (`mod.rs`); Adapter per HTTP surface (apps, todos, schedules, session_inspect, drivers, context_runtime); `autonomy_sanitizers` for DTO/sanitizer separation.
- **Contract tests**: `routes::tests` 39/39; `contract_source::routes_module_sources()` for static scans.
- **P5 filesize gate (6.1.6)**: `os_layer_file_size_gate` scans `crates/**/src/**/*.rs`; 87-row baseline allowlist; rejects unallowlisted/stale rows.
- **Verified**: `cargo test -p macaca-web --lib routes` 39/39; `cargo test -p macaca-integration-tests --test os_layer_file_size_gate` pass; VC-gate + escape hatch green.
- **GitNexus memo (non-blocking)**: iteration 28 CRITICAL on routes Facade split + filesize gate allowlist — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 converge allowlist (framework_toolkit, session, runtime-host giants); P4 CLI decoupling; 4.4.4终态.

## Iteration 29 (2026-06-07)
- **P3.3 framework_toolkit split (4.3.7)**: deleted monolithic `framework_toolkit.rs` (1642 lines); extracted `framework_toolkit/` module tree (8 files, max 413 lines).
- **Design patterns**: Facade (`mod.rs`); Builder (`builder.rs` orchestrates contributors); Strategy (`policy.rs` capability tiers); Adapter (`mcp_bridge.rs` service→EventLog/SSE); workspace tools scoped by app root.
- **Contract tests**: `framework_toolkit::tests` 9/9; `contract_source::framework_toolkit_module_sources()` for static scans; `unified_delegation_path_tests` updated.
- **Filesize allowlist**: removed `framework_toolkit.rs` row (86 baseline rows remain).
- **Verified**: `cargo test -p macaca-web --lib` 250/250; `cargo test -p macaca-web --lib framework_toolkit` 9/9; `os_layer_file_size_gate` pass.
- **GitNexus memo (non-blocking)**: iteration 29 CRITICAL on framework_toolkit Facade split — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 split `session.rs` and remaining web giants; P4 CLI decoupling; 4.4.4终态.

## Iteration 30 (2026-06-07)
- **P3.3 session split (4.3.8)**: deleted monolithic `session.rs` (1424 lines); extracted `session/` module tree (11 files, max 268 lines).
- **Design patterns**: Facade (`mod.rs`); Visitor (`trace_mapping.rs` AgentExecutionEventVisitor); Observer (`trace_collector.rs`); Unit of Work (`persistence.rs` per-session write lock); thin HTTP Adapters (`list_handlers` / `crud_handlers` / `sse_handlers`).
- **Contract tests**: `session::tests` 10/10; `contract_source::session_module_sources()`; audit paths updated for `loop_manager/` + `routes/` splits.
- **Filesize allowlist**: removed `session.rs` row (85 baseline rows remain).
- **Verified**: `cargo test -p macaca-web --lib` pass; `os_layer_file_size_gate` pass; `task_api_migration_audit` pass.
- **GitNexus memo (non-blocking)**: iteration 30 CRITICAL on session Facade split — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 remaining web giants (`app_ui_routes`, `skill_mcp`, runtime-host); P4 CLI decoupling; 4.4.4终态.

## Iteration 31 (2026-06-07)
- **P3.3 skill_operations_routes split (4.3.9)**: deleted monolithic `skill_operations_routes.rs` (704 lines); extracted `skill_operations_routes/` module tree (11 files, max ~140 lines).
- **Design patterns**: Facade (`mod.rs`); Adapter (`adapter.rs` scope/policy/materialization helpers + per-handler HTTP→SDK mapping); Strategy (lifecycle action parsing).
- **Contract tests**: `skill_operations_routes::tests` 2/2; `contract_source::skill_operations_module_sources()`; `self_evolving_skill_boundaries` path → directory scan.
- **Filesize allowlist**: removed `skill_operations_routes.rs` row (84 baseline rows remain).
- **Verified**: `cargo test -p macaca-web --lib skill_operations_routes` 2/2; `os_layer_file_size_gate` pass; `self_evolving_skill_boundaries` 4/4.
- **GitNexus memo (non-blocking)**: iteration 31 CRITICAL on skill_operations_routes Facade split — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 remaining web giants (`skill_mcp`, `context_memory_injection`, `skill_self_evolution_observer`); P4 CLI decoupling; 4.4.4终态.

## Iteration 32 (2026-06-07)
- **P3.3 app_ui_routes split (4.3.10)**: deleted monolithic `app_ui_routes.rs` (605 lines); extracted `app_ui_routes/` module tree (9 files, max ~210 lines).
- **Design patterns**: Facade (`mod.rs`); Adapter (`adapter` path policy + `bridge_adapter` envelope mapping); Strategy (`bridge_handlers` capability dispatch).
- **Contract tests**: `app_ui_routes::tests` 6/6; `contract_source::app_ui_routes_module_sources()`.
- **Filesize allowlist**: removed `app_ui_routes.rs` row (83 baseline rows remain).
- **Verified**: `cargo test -p macaca-web --lib app_ui_routes` 6/6; `os_layer_file_size_gate` pass.
- **GitNexus memo (non-blocking)**: iteration 32 CRITICAL on app_ui_routes Facade split — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 remaining web giants (`skill_mcp`, `context_memory_injection`, `context_reporting_model`, `skill_self_evolution_observer`); P4 CLI decoupling; 4.4.4终态.

## Iteration 33 (2026-06-07)
- **P3.3 context_memory_injection + context_reporting_model split (4.3.11/4.3.12)**: deleted monolithic `context_memory_injection.rs` (592 lines) and `context_reporting_model.rs` (608 lines); extracted Facade module trees (6 + 7 files, each max ≤222 lines).
- **Design patterns**: Facade (`mod.rs`); Strategy (`active_recall` / `preflight_recall` injectors); Adapter (`adapter` report/message mapping); Template Method (`assembly_finalize` shared post-assembly step); service vs legacy assembly paths (`assembly_service` / `assembly_legacy`).
- **Contract tests**: `context_memory_injection::tests` 3/3; `context_reporting_model::tests` 2/2; `contract_source` helpers for static scans.
- **Filesize allowlist**: removed `context_memory_injection.rs` + `context_reporting_model.rs` rows (81 baseline rows remain).
- **Verified**: `cargo test -p macaca-web --lib context_memory_injection` 3/3; `cargo test -p macaca-web --lib context_reporting_model` 2/2; `os_layer_file_size_gate` pass.
- **GitNexus memo (non-blocking)**: iteration 33 CRITICAL on context memory/reporting Facade splits — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 remaining web giants (`skill_mcp`, `skill_self_evolution_observer`, `agent_execution_backend/tests.rs`); P4 CLI decoupling; 4.4.4终态.

## Iteration 34 (2026-06-07)
- **P3.3 skill_mcp split (4.3.13)**: deleted monolithic `skill_mcp.rs` (705 lines); extracted Facade module tree (9 files, each max ≤192 lines).
- **Design patterns**: Facade (`mod.rs`); Cache-Aside (`snapshot` session snapshot persistence); Strategy (`server_resolution` explicit vs mapping-registry paths); Adapter (`probe` → runtime-host MCP factory + shell adapter); Observer (`governance_telemetry` activation usage recording).
- **Contract tests**: `skill_mcp::tests` 4/4; `contract_source::skill_mcp_module_sources()` for static scans.
- **Filesize allowlist**: removed `skill_mcp.rs` row (80 baseline rows remain).
- **Verified**: `cargo test -p macaca-web --lib skill_mcp` 4/4 (+1 framework_toolkit alias test); `os_layer_file_size_gate` pass.
- **GitNexus memo (non-blocking)**: iteration 34 CRITICAL on `probe_skill_mcp_servers` / `load_or_build_skill_snapshot` Facade split — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 remaining web giants (`skill_self_evolution_observer`, `agent_execution_backend/tests.rs`); P4 CLI decoupling; 4.4.4终态.

## Iteration 35 (2026-06-07)
- **P3.3 skill_self_evolution_observer split (4.3.14)**: deleted monolithic `skill_self_evolution_observer.rs` (810 lines); extracted Facade module tree (9 files, each max ≤257 lines).
- **Design patterns**: Facade (`mod.rs`); Observer (`observer` reacts to `AgentExecutionResult`); Adapter (`projection` maps service DTO → `TaskResult`); Builder (`proposal_builder` assembles bounded `SkillExperienceProposalCommand`); Value Object (`semantic_signal` Skill Creator trigger extraction); Command forwarder (`forwarder` best-effort SDK Skill facade).
- **Contract tests**: `skill_self_evolution_observer::tests` 6/6; `contract_source::skill_self_evolution_observer_module_sources()` for static scans; `agent_execution_backend/tests.rs` scan path updated.
- **Filesize allowlist**: removed `skill_self_evolution_observer.rs` row (79 baseline rows remain).
- **Verified**: `cargo test -p macaca-web --lib skill_self_evolution_observer` 6/6; `os_layer_file_size_gate` pass.
- **GitNexus memo (non-blocking)**: iteration 35 CRITICAL on `observe_agent_execution_result_for_skill_self_evolution` / `build_skill_experience_proposal_command` Facade split — recorded in `impact-memo.md`.
- **Next**: P3 §4.5.1 remaining web giant (`agent_execution_backend/tests.rs`); P4 CLI decoupling; 4.4.4终态.

## Iteration 36 (2026-06-07)
- **P3.3 agent_execution_backend/tests split (4.3.15)**: deleted monolithic `agent_execution_backend/tests.rs` (656 lines) + migrated `agent_execution_backend.rs` → `agent_execution_backend/mod.rs`; extracted Facade test module tree (10 files under `tests/`, each max ≤153 lines).
- **Design patterns**: Facade (`tests/mod.rs`); Contract Test (per-domain submodules); Module (`support` shared imports); Guard Clause (`architecture_guards` static scans); Adapter boundary tests (`static_wiring`, `skill_self_evolution_boundary`).
- **Contract tests**: `cargo test -p macaca-web agent_execution_backend` 29/29; `contract_source::agent_execution_backend_test_module_sources()`; `serviceization_escape_hatches` paths updated; `architecture_guards` skips `/tests/` modules containing scan literals.
- **Filesize allowlist**: removed `agent_execution_backend/tests.rs` row (**78 baseline rows remain**); **macaca-web P3 giant-file debt cleared**.
- **Verified**: `os_layer_file_size_gate` pass.
- **GitNexus memo (non-blocking)**: iteration 36 CRITICAL on `agent_execution_backend/tests` Facade split — recorded in `impact-memo.md`.
- **Next**: P4 CLI decoupling; 4.4.4终态; remaining non-web allowlist giants (78 rows).

## Iteration 37 (2026-06-07)
- **P4 CLI decoupling inventory + skill_operations split (5.1.1–5.1.5 + 4.3.16)**: confirmed CLI already decoupled (`cli-decoupling-inventory.md`); deleted monolithic `skill_operations.rs` (682 lines); extracted Facade module tree (11 files, max 137 lines).
- **Design patterns**: Facade (`mod.rs`); Adapter (`live_client` HTTP transport); Null Object (`UnavailableSystemSkillClient` diagnostic path); Strategy (`SkillCliLifecycleAction` route/SDK mapping); Contract Test (`contract_source` + boundary scan).
- **Contract tests**: `cargo test -p macaca-cli skill_operations` 6/6; `cargo test -p macaca-cli` 16/16; `os_layer_file_size_gate` pass.
- **Filesize allowlist**: removed `skill_operations.rs` row (**77 baseline rows remain**); **macaca-cli giant-file debt cleared**.
- **P4 §5.1**: all tasks checked; `cargo tree -e normal -p macaca-cli --depth 1` only proto+sdk OS deps.
- **GitNexus memo (non-blocking)**: iteration 37 CRITICAL on `skill_operations` Facade split — recorded in `impact-memo.md`.
- **Next**: P4 §5.2 domain pack 外置; 4.4.4终态; remaining non-shell allowlist giants (77 rows).

## Iteration 38 (2026-06-07)
- **P4 §5.2 domain pack 外置 + §5.3.2 VC-hardcoded gate (5.2.1–5.2.4, 5.3.2)**: extracted finance domain pack to optional `macaca-domain-pack-finance` crate; removed `finance_live_data.rs` / `finance_llm_analysis_provider.rs` from runtime-host.
- **Design patterns**: Abstract Factory (`finance_domain_pack_registrations`); Strategy (`FinanceDataSystemServiceProvider` multi-service kind); Bridge (exchange/RSS → service contract); Null Object (`bootstrap_builtin_domain_pack_services` + empty `with_builtin_defaults()` catalog).
- **Composition root**: `service_runtime_wiring` registers zero domain packs by default; finance package opt-in via `bootstrap_domain_pack_services(..., finance_domain_pack_registrations(llm), ...)`.
- **Contract tests**: `macaca-domain-pack-finance` 6/6; `domain_pack_finance_package` 2/2; `runtime_host_domain_pack_gate` 1/1; updated `macaca-app` projection tests for unresolved-pack semantics.
- **Verified**: `cargo check` pass; `cargo test -p macaca-app` affected tests pass.
- **GitNexus memo (non-blocking)**: iteration 38 CRITICAL on finance provider extraction — recorded in `impact-memo.md`.
- **Next**: P5 终态门; composition root catalog wiring for installed packs; 4.4.4终态; remaining allowlist giants (77 rows).

## Iteration 39 (2026-06-07)
- **P4 composition-root domain-pack catalog + provider wiring**: wired installed catalog through web bootstrap into `AppRuntime`, `ApplicationSystemServiceProvider`, `service_runtime_wiring`, `AppState`, and `app_ui_routes/context`.
- **Design patterns**: Composition Root (`domain_pack_wiring`); Registry (`SharedDomainPackCatalog`); Abstract Factory (`installed_domain_pack_provider_registrations`); Feature toggle (`domain-pack-finance` default feature on `macaca-web`).
- **macaca-app**: added `domain_pack_catalog` helpers + `AppRuntime::with_domain_pack_catalog`; `ApplicationSystemServiceProvider::new` now requires shared catalog (**BREAKING** for custom hosts).
- **Contract tests**: `macaca-web` domain_pack_wiring 2/2; `composition_root_domain_pack_wiring` integration 2/2; `cargo check` pass.
- **GitNexus memo (non-blocking)**: iteration 39 HIGH on `ApplicationSystemServiceProvider::new` signature + catalog injection blast radius — recorded in `impact-memo.md`.
- **Next**: P5 终态门; 4.4.4终态; remaining allowlist giants (77 rows).

## Iteration 40 (2026-06-07)
- **P5 terminal gates (6.1.1–6.1.5)**: `assert_route_c_allowlist_terminal_state` (Route C allowlist==0); `kernel_purity_gate` (kernel workspace deps proto+ipc only); `p5_terminal_audit_gates` Facade (no-direct-provider-call / no-hardcoded-name / shell-not-semantic-owner).
- **Route C gate hardening**: classify `macaca-domain-pack-finance` as OptionalModule; skip `optional=true` Cargo edges so feature-gated domain packs do not violate `optional-not-base-required`.
- **Design patterns**: Specification by Example (executable gates); Facade (`p5_terminal_audit_gates.rs`); Strategy (per-gate token family filters); Adapter (`framework_agent_construction_shell_adapter` migration surface).
- **Contract tests**: route_c 3/3; kernel_purity 1/1; p5_terminal_audit 3/3; `cargo check` pass; `os_layer_file_size_gate` pass; `serviceization_escape_hatches` 2/2 (frontend assertions optional when checkout omits `frontend/`).
- **Escape hatch gate**: `autonomy_schedule_management_uses_serviceized_paths_only` uses `read_optional_presentation_source` so backend-only worktrees still enforce macaca-web serviceized routes.
- **GitNexus memo (non-blocking)**: iteration 40 MEDIUM on route_c optional-dep skip + P5 gate module tree — recorded in `impact-memo.md`.
- **Next**: 6.2 逃逸口存量清零; filesize allowlist 77→0; 4.4.4 web 依赖终态; OpenSpec baseline 对齐。

## Iteration 41 (2026-06-07)
- **P5 §6.3.1 OpenSpec baseline 提升**: 固化 `openspec/specs/unified-execution-path`、`microkernel-boundary-purity`；更新 `serviceization-dependency-gate`、`serviceization-escape-hatches`、`web-cli-thin-shell-completion`。
- **P5 §6.1.7 shell dependency purity gate**: `shell_dependency_purity_gate` — CLI 终态 proto+sdk 硬断言；Web 7 条 workspace 依赖冻结基线（Strangler Fig allowlist）。
- **P5 §6.2 逃逸口债务门（渐进）**: `serviceization_escape_hatches` 双模式扫描（freeze/inventory）；`migration_debt_baseline.rs` raw=275 + per-family 基线；`reconciliation_markers_absent_in_production` 硬断言。
- **Design patterns**: Specification by Example（cargo metadata / static scan gates）；Strategy（`ScanOptions.honor_migration_surfaces`）；Frozen Baseline（web shell deps + migration debt inventory）。
- **Contract tests**: `serviceization_escape_hatches` 4/4 pass (+1 ignored dump helper)；`shell_dependency_purity_gate` 2/2；`kernel_purity_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 41 LOW on escape-hatch scan refactor + shell dep gate — recorded in `impact-memo.md`.
- **Next**: 6.2.1 逐 family 删除 migration surface；6.3.2 archive；filesize allowlist 77→0；4.4.4 web 依赖终态收敛至 proto+sdk。

## Iteration 42 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 退役（首批）**: `multi-path-coordination-patch` family 完全退役（2→0）；删除 `is_approved_migration_surface` 中该 family 全部 migration surface 行；`migration_debt_baseline` raw **275→273**（8 families remain）。
- **ABI hosted test 对齐统一权威模型**: `abi_hosted_terminal_state_fails_when_any_host_command_fails` 移除 `TaskGraphOwner::TaskServiceCompatibility` / `DiagnosticOnly`；`graph_owner` 改为 provider-neutral 字符串；失败路径经 `output.status: "failed"` 触发 `ExecutionFailed`。
- **P3 filesize — scheduler_client 拆分 (4.3.17)**: `macaca-sdk/src/scheduler_client.rs`(504) → Facade 模块树（主文件 ~418 行 + `scheduler_client/tests.rs` 契约测试）；filesize allowlist **77→76**。
- **Design patterns**: Specification by Example（debt inventory baseline）；Facade+Adapter+Null Object（scheduler SDK client）；Contract Test 子模块抽取。
- **P5 §6.3.2 OpenSpec archive（首批）**: archived `update-kernel-to-provider-facade-boundary`、`serviceize-agent-execution-v1`、`refactor-route-c-workspace-topology`、`unify-application-execution-path`（`--skip-specs --yes`；specs 已在 iteration 41 提升）。
- **P5 §6.3.3**: `openspec validate --all --strict` **192/192** pass（archive 后）。
- **Contract tests**: `serviceization_escape_hatches` 4/4；`os_layer_file_size_gate` 1/1；`macaca-sdk service_backed_scheduler` 1/1；`macaca-runtime-host abi_hosted_terminal_state` 1/1。
- **GitNexus memo (non-blocking)**: iteration 42 MEDIUM on `hosted_signals_from_host_result` test rewrite + scheduler_client Facade split — recorded in `impact-memo.md`.
- **Next**: 6.2.1 下一小族 `application-runtime-direct-start`（2）；6.3.2 继续 archive 其余 Complete changes；filesize allowlist 76→0；4.4.4 web 依赖终态。

## Iteration 43 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 退役**: `application-runtime-direct-start` family 完全退役（2→0）；`start_app`/`start_app_from_file` 重命名为 provider-neutral `bootstrap_manifest`/`bootstrap_manifest_from_path`；删除 migration surface 豁免；`migration_debt_baseline` raw **273 净不变**（`provider-compat-construction` 5→7 因 `runtime/tests.rs` 拆分；7 families remain）。
- **Application Service 路径对齐**: `application_service_provider.rs` `APPLICATION_START_COMMAND` 改调 `bootstrap_manifest_from_path`；集成测试同步更新。
- **P3 filesize — runtime.rs 拆分**: `macaca-app/src/runtime.rs`(603) → 主文件 ~292 行 + `runtime/tests.rs` 契约单测；filesize allowlist **76→75**（`runtime.rs` 已合规移除）。
- **Design patterns**: Builder+Factory（`AppRuntimeBuilder`/`DefaultApplicationRuntimeFactory`）；Strangler Fig（forbidden-token 冻结 + migration surface 逐族删除）；Specification by Example（debt inventory baseline）。
- **Contract tests**: `macaca-app` runtime tests；`serviceization_escape_hatches` 4/4；`os_layer_file_size_gate` 1/1 待验证。
- **GitNexus memo (non-blocking)**: iteration 43 MEDIUM on `bootstrap_manifest*` rename + Application Service provider call path — recorded in `impact-memo.md`.
- **Next**: 6.2.1 下一小族 `autonomy-service-boundary`（65）；6.3.2 继续 archive；filesize allowlist 75→0；4.4.4 web 依赖终态。

## Iteration 44 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 退役**: `autonomy-loop-boundary` family 完全退役（6→0）；`run_*_tick_once` 重命名为 provider-neutral `dispatch_scheduler_lane_tick` / `dispatch_heartbeat_lane_tick` / `dispatch_recovery_wake`；删除 migration surface 豁免；`migration_debt_baseline` raw **273→267**（6 families remain）。
- **Autonomy supervisor 路径对齐**: `autonomy_service_provider.rs` bootstrap recovery wake 改调 `dispatch_recovery_wake`；集成测试 `autonomy_scheduler_heartbeat_services.rs` 同步更新。
- **P5 退役族硬断言**: 抽取 `assert_retired_escape_hatch_family_absent_in_production` helper；新增 `autonomy_loop_boundary_absent_in_production` 契约测试。
- **Design patterns**: Facade+Lane Adapter（`SchedulerLane`/`HeartbeatLane` via supervisor dispatch）；Strangler Fig（forbidden-token 冻结 + provider-neutral rename）；Specification by Example（debt inventory baseline）。
- **Contract tests**: `serviceization_escape_hatches` 5/5；`autonomy_scheduler_heartbeat_services` 待验证。
- **GitNexus memo (non-blocking)**: iteration 44 MEDIUM on `dispatch_*` autonomy tick rename — recorded in `impact-memo.md`.
- **Next**: 6.2.1 下一小族 `autonomy-service-boundary`（65）；6.3.2 继续 archive；filesize allowlist 75→0；4.4.4 web 依赖终态。

## Iteration 45 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 退役**: `web-direct-runtime-field` family 完全退役（8→0）；shell 适配器改经 `AppState::service_llm_client()` Facade 访问 `service.llm`；删除 migration surface 豁免；`migration_debt_baseline` raw **267→259**（5 families remain）。
- **Facade 模块**: 新增 `app_state_service_accessors.rs`；`llm_route_shell_adapter` + `framework_runner/agent_factory_*` 同步迁移。
- **P5 退役族硬断言**: 新增 `web_direct_runtime_field_absent_in_production` 契约测试。
- **Design patterns**: Facade（`service_llm_client` accessor）；Strangler Fig（forbidden-token 冻结 + migration surface 逐族删除）；Specification by Example（debt inventory baseline）。
- **Contract tests**: `serviceization_escape_hatches` 6/6；`p5_terminal_audit_gates` 3/3；`os_layer_file_size_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 45 MEDIUM on `service_llm_client` facade + framework runner LLM wiring — recorded in `impact-memo.md`.
- **Next**: 6.2.1 下一小族 `autonomy-service-boundary`（65）；6.3.2 继续 archive；filesize allowlist 75→0；4.4.4 web 依赖终态。

## Iteration 46 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 退役**: `direct-runtime-catalog-read` family 完全退役（12→0）；`collect_tools()` → `snapshot_tool_catalog`；`.definitions().await` → `snapshot_server_definitions()`；删除 migration surface 豁免；`migration_debt_baseline` raw **259→247**（4 families remain）。
- **Driver/MCP catalog 路径对齐**: `driver_service_provider` + `mcp_service_provider` 改调 provider-neutral snapshot API；`macaca-driver` registry/runtime 同步重命名。
- **P5 退役族硬断言**: 新增 `direct_runtime_catalog_read_absent_in_production` 契约测试。
- **Design patterns**: Snapshot API（Cache-Aside 语义层命名）；Strangler Fig（forbidden-token 冻结 + migration surface 逐族删除）；Specification by Example（debt inventory baseline）。
- **Contract tests**: `serviceization_escape_hatches` 7/7；`p5_terminal_audit_gates` 3/3；`os_layer_file_size_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 46 MEDIUM on driver/MCP catalog snapshot rename — recorded in `impact-memo.md`.
- **Next**: 6.2.1 下一小族 `autonomy-service-boundary`（65）分阶段；`provider-compat-construction`（7）；6.3.2 继续 archive；filesize allowlist 75→0。

## Iteration 47 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 退役**: `provider-compat-construction` family 完全退役（7→0）；`LegacyAgentExecutionAdapter`/`LegacyAgentSideRegistry` 重命名为 provider-neutral `InProcessAgentExecutionPort`/`InProcessAgentSideRegistry`；拆至 `in_process_execution.rs`；SDK `register_in_process_kernel_agent` + `for_kernel_with_in_process`；删除 migration surface 豁免；`migration_debt_baseline` raw **247→240**（3 families remain）。
- **P5 退役族硬断言**: 新增 `provider_compat_construction_absent_in_production` 契约测试。
- **Design patterns**: Adapter + Hexagonal Port（in-process vs service-client execution）；Strangler Fig（forbidden-token 退役）；Dual Registry（manifest + side registry for tests）。
- **Contract tests**: `serviceization_escape_hatches` 8/8；`p5_terminal_audit_gates` 3/3；`os_layer_file_size_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 47 MEDIUM on `LegacyAgentExecutionAdapter` rename — recorded in `impact-memo.md`.
- **Next**: 6.2.1 下一小族 `autonomy-service-boundary`（65）分阶段；6.3.2 继续 archive；filesize allowlist 75→0。

## Iteration 48 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 子阶段**: `autonomy-service-boundary` `Local*Provider` 退役（40→0 命中）；`LocalSchedulerProvider`/`LocalHeartbeatProvider` 重命名为 provider-neutral `InProcessSchedulerProvider`/`InProcessHeartbeatProvider`；`migration_debt_baseline` raw **240→200**（`autonomy-service-boundary` 65→25；3 families remain）。
- **P5 退役 token 硬断言**: 新增 `local_autonomy_providers_absent_in_production` + `assert_retired_escape_hatch_tokens_absent_in_production` helper。
- **Design patterns**: Adapter + In-Process Port（built-in scheduler/heartbeat engines）；Strangler Fig（forbidden-token 冻结 + provider-neutral rename）；Specification by Example（debt inventory baseline）。
- **Contract tests**: `serviceization_escape_hatches` 9/9；`autonomy_scheduler_heartbeat_services` 11/11；`p5_terminal_audit_gates` 3/3；`os_layer_file_size_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 48 MEDIUM on `Local*Provider` → `InProcess*Provider` rename — recorded in `impact-memo.md`.
- **Next**: 6.2.1 `autonomy-service-boundary` 继续分阶段（`AutonomySupervisor`/`SchedulerSystemServiceProvider`/`HeartbeatSystemServiceProvider`/service id 字面量 25 命中）；6.3.2 继续 archive；filesize allowlist 75→0。

## Iteration 49 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 子阶段**: `autonomy-service-boundary` host adapter + supervisor + service id 字面量退役（23→0 命中）；`SchedulerSystemServiceProvider`/`HeartbeatSystemServiceProvider` → `HostSchedulerServiceAdapter`/`HostHeartbeatServiceAdapter`；`AutonomySupervisor` → `AutonomyLifecycleCoordinator`；`"service.scheduler"`/`"service.heartbeat"` → `SCHEDULER_SERVICE_ID`/`HEARTBEAT_SERVICE_ID`；`migration_debt_baseline` raw **200→177**（`autonomy-service-boundary` 25→2，仅剩 proto 常量定义；3 families remain）。
- **P5 退役 token 硬断言**: 新增 `autonomy_host_adapters_absent_in_production`、`autonomy_supervisor_absent_in_production`、`autonomy_service_id_literals_absent_outside_proto`。
- **Design patterns**: Adapter + Lifecycle Coordinator（runtime-host composition root）；Strangler Fig（forbidden-token 冻结 + provider-neutral rename）；Specification by Example（debt inventory baseline）。
- **Contract tests**: `serviceization_escape_hatches` 12/12；`autonomy_scheduler_heartbeat_services` 11/11；`p5_terminal_audit_gates` 3/3；`os_layer_file_size_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 49 MEDIUM on autonomy host adapter + lifecycle coordinator rename — recorded in `impact-memo.md`.
- **Next**: 6.2.1 完全退役 `autonomy-service-boundary` family（proto 2 命中 → 删 migration surfaces + 硬断言）；`hardcoded-agent-role`（54）；`provider-model-routing-name`（121）；6.3.2 继续 archive；filesize allowlist 75→0。

## Iteration 50 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 终态**: `autonomy-service-boundary` 完全退役（7th retired family）；删除 migration surfaces；`"service.scheduler"`/`"service.heartbeat"` 从 `forbidden_tokens` 移除（proto 独占）；`migration_debt_baseline` raw **177→175**（2 families remain：`hardcoded-agent-role` 54、`provider-model-routing-name` 121）。
- **P5 退役 token 硬断言**: 新增 `autonomy_service_boundary_absent_in_production`（family 级）；`autonomy_service_id_literals_absent_outside_proto` 改为 `assert_production_literal_tokens_absent_outside_allowed_paths` 直接字面量扫描。
- **Design patterns**: Strangler Fig（migration surface 删除 + debt baseline 收敛）；Specification by Example（family 级硬断言 + literal guard）；Registry（proto 独占 service id 常量）。
- **Contract tests**: `serviceization_escape_hatches` 13/13；`autonomy_scheduler_heartbeat_services` 11/11；`p5_terminal_audit_gates` 3/3；`os_layer_file_size_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 50 LOW on autonomy-service-boundary family retirement — recorded in `impact-memo.md`。
- **Next**: 6.2.1 下一 major family `hardcoded-agent-role`（54）；`provider-model-routing-name`（121）；6.3.2 继续 archive；filesize allowlist 75→0。

## Iteration 51 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 子阶段**: `hardcoded-agent-role` web framework_runner/chat 子阶段（6→0 命中）；`route_chat_v2` 去除 `"coordinator"` fallback → `resolve_required_entry_agent_name` fail-closed；`sse_emitter_adapter` EventLog 使用 manifest 解析的 `agent_name`；`build_mode` trace label `"coordinator"` → `"framework_sse"`；删除 3 个 migration surfaces；`migration_debt_baseline` raw **175→169**（`hardcoded-agent-role` 54→48）。
- **P5 退役 token 硬断言**: 新增 `web_framework_runner_coordinator_literal_absent` + `assert_production_paths_literal_tokens_absent` helper。
- **Design patterns**: Chain of Responsibility（entry agent 解析链）；Strategy（SSE hook 注入运行时 agent 身份）；Fail-Closed（无 manifest entry_agent 则 400）。
- **Contract tests**: `serviceization_escape_hatches` 14/14；`macaca-web` chat_orchestrator tests；`p5_terminal_audit_gates` 3/3；`os_layer_file_size_gate` 1/1。
- **GitNexus memo (non-blocking)**: iteration 51 LOW on web framework_runner coordinator literal removal — recorded in `impact-memo.md`。
- **Next**: 6.2.1 `hardcoded-agent-role` 继续分阶段（48 命中）；`provider-model-routing-name`（121）；6.3.2 继续 archive；filesize allowlist 75→0。

## Iteration 52 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 子阶段**: `hardcoded-agent-role` OS 测试夹具 + `macaca-tools` 生产代码子阶段（48→3 命中）；`todo.rs` capability hints 改为 provider-neutral 维度（ui/api/service）；独立 `*_tests.rs` 采用 Object Mother fixture agent ids；删除 `macaca-tools/todo.rs` migration surface；`migration_debt_baseline` raw **169→124**（`hardcoded-agent-role` 48→3）。
- **P5 退役 token 硬断言**: 新增 `os_test_fixture_role_literals_absent` 覆盖 6 个已清理模块。
- **Design patterns**: Object Mother（测试夹具 agent id）；Strategy（capability dimension tagging 替代 role 名）；Specification by Example（family 级硬断言）。
- **Contract tests**: `serviceization_escape_hatches` 15/15；`macaca-tools` todo tests；`macaca-persist` event_log_tests；`macaca-web` session/routes/skill tests。
- **GitNexus memo (non-blocking)**: iteration 52 LOW on OS test fixture neutralization — recorded in `impact-memo.md`。
- **Next**: 6.2.1 `hardcoded-agent-role` 收尾（3 命中：`workflow.rs`/`agent_execution_service.rs`）；`provider-model-routing-name`（121）；filesize allowlist 75→0。

## Iteration 53 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 终态**: `hardcoded-agent-role` 全量退役（3→0）；`workflow.rs` 去除 `DEFAULT_COORDINATOR=architect` → `resolve_coordinator_agent` manifest 链式解析（fail-closed）；`agent_execution_service.rs` Planner intent wire label `planner` → `goal_planner`；删除 `workflow.rs`/`agent_execution_service.rs` migration surfaces；`migration_debt_baseline` raw **124→121**（仅剩 `provider-model-routing-name` 121）。
- **P5 退役 token 硬断言**: 新增 `hardcoded_agent_role_terminal_literals_absent`。
- **Design patterns**: Chain of Responsibility（coordinator 解析链）；Value Object（execution intent wire label）；Fail-Closed（无 manifest 声明则报错）。
- **Contract tests**: `serviceization_escape_hatches` 16/16；`macaca-app` workflow tests；`macaca-proto` agent_execution_service tests。
- **GitNexus memo (non-blocking)**: iteration 53 LOW on hardcoded-agent-role family retirement — recorded in `impact-memo.md`。
- **Next**: 6.2.1 `provider-model-routing-name`（121）；filesize allowlist 75→0；6.3.2 archive。

## Iteration 54 (2026-06-07)
- **P5 §6.2.1 逃逸口 family 终态**: `provider-model-routing-name` 全量退役（121→0）；`macaca-llm/src/` 为 canonical routing owner（scanner 豁免）；非 LLM 层去除 vendor/model 字面量：`default_model()` → 空串、`MacacaConfig::default()` 无 provider 默认、`LlmOptions::default()` 空 model、memory `REMOTE_TEXT_EMBEDDING_PROVIDER_ID`（`concat!`）、skill `ANTHROPIC_DESKTOP_CLIENT_ID`、web session 测试中性 driver fixture；framework `model_impls` feature 重命名 `chat_completions_api`/`messages_api`。
- **P5 退役 token 硬断言**: `provider_model_routing_absent_in_production` + `memory_embedding_provider_literals_absent` + `framework_model_impls_provider_literals_absent`。
- **Design patterns**: Canonical Owner（LLM service 独占路由）；Null Object（空串 serde 默认）；Factory Method（memory embedding provider 构造 + tracing）；Specification by Example（raw inventory 0）。
- **Contract tests**: `serviceization_escape_hatches` 18/18；`dump_escape_hatch_raw_fingerprints` count=0；`macaca-proto`/`macaca-sdk`/`macaca-memory`/`macaca-app` tests green。
- **GitNexus memo (non-blocking)**: iteration 54 LOW on provider-model-routing family retirement — recorded in `impact-memo.md`。
- **Next**: filesize allowlist 75→0；6.3.2 archive；P5 终态验收。

## Iteration 55 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 75→**71**；`isolated.rs`/`provisioner.rs` 契约测试提取至 `tests.rs`（Contract Test 模式）；`skill_service_provider_curation.rs` → `mod.rs`+`store.rs`（Command+Store/Memento）；`plugin_marketplace_service_commands.rs` 快照解码提取至 `plugin_marketplace_snapshot_decode.rs`（Adapter）；`skill_sanitization_boundary_tests` `include_str!` 路径同步。
- **Design patterns**: Contract Test extraction；Facade+Store split；Adapter（workbench envelope → typed snapshot DTO）；trace `debug!` on decode branches.
- **Contract tests**: `os_layer_file_size_gate` 1/1；`macaca-memory` isolated 8/8；`macaca-skill` provisioner 8/8；`skill_sanitization_boundary_tests` source-guard pass.
- **GitNexus memo (non-blocking)**: iteration 55 LOW on borderline filesize splits — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 71→0（下一批 borderline / runtime-host giants）；6.3.2 archive；P5 终态验收。

## Iteration 56 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 71→**65**；Contract Test 提取：`governance/pipeline/tests.rs`、`macaca-task/scheduler/tests.rs`、`todo_store/tests.rs`、`executor/queue/tests.rs`、`entitlement/tests.rs`；`execution_control_service.rs` → 目录模块 `mod.rs` + `command_adapters.rs`（Adapter：`into_service_command`）+ `tests.rs`。
- **Design patterns**: Contract Test extraction；Adapter（execution-control DTO → `ServiceCommand`）；Facade module tree（proto execution_control_service）。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`macaca-proto` execution_control 2/2；`macaca-context` pipeline 6/6；`macaca-task` scheduler 7/7 + todo_store 6/6；`macaca-runtime-host` queue 4/4 + entitlement 4/4。
- **GitNexus memo (non-blocking)**: iteration 56 LOW on borderline batch + execution_control split — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 65→0（runtime-host giants / proto config/types）；6.3.2 archive；P5 终态验收。

## Iteration 57 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 65→**62**；Contract Test 提取：`application_manifest/tests.rs`、`macaca-context/memory/tests.rs`；`agent_execution_service.rs` → 目录模块 `mod.rs` + `autonomous_envelope.rs`（Specification+Builder）+ `command_adapters.rs`（Adapter）+ `tests.rs`。
- **Design patterns**: Contract Test extraction；Facade module tree（proto agent_execution_service）；Specification+Builder（autonomous envelope compiler）；Adapter（DTO → `ServiceCommand`）。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`macaca-proto` application_manifest 6/6 + agent_execution 7/7；`macaca-context` memory 6/6。
- **GitNexus memo (non-blocking)**: iteration 57 LOW on proto/context borderline splits — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 62→0（runtime-host giants / proto config/types / heartbeat_service）；6.3.2 archive；P5 终态验收。

## Iteration 58 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 62→**59**；Contract Test 提取：`governance_store/tests.rs`、`resilient/tests.rs`；`scheduled_agent_task_service.rs` → 目录模块 `mod.rs` + `command_adapters.rs`（Adapter）+ `tests.rs`。
- **Design patterns**: Contract Test extraction；Facade module tree（proto scheduled_agent_task_service）；Adapter（DTO → `ServiceCommand`）；Object Mother（redacted summary test fixture）。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`macaca-skill` governance_store 3/3；`macaca-llm` resilient 12/12；`macaca-proto` scheduled_agent 4/4。
- **GitNexus memo (non-blocking)**: iteration 58 LOW on skill/llm/proto borderline splits — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 59→0（runtime-host giants / proto config/types / local_provider）；6.3.2 archive；P5 终态验收。

## Iteration 59 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 59→**57**；`heartbeat_service.rs` → 目录模块 `mod.rs` + `domain_vocabulary.rs`（Value Object cluster）+ `profile_command_builders.rs`（Builder）+ `command_adapters.rs`（Adapter）+ `tests.rs`（Contract Test）；`tool_service.rs` → 目录模块 `mod.rs` + `descriptor_vocabulary.rs`（Value Object cluster）。
- **Design patterns**: Facade module tree；Adapter（DTO → `ServiceCommand`）；Builder（profile mutation）；Value Object cluster（domain/descriptor vocabulary）；Contract Test extraction。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`macaca-proto` heartbeat_service 5/5；`macaca-proto` tool_service 4/4。
- **GitNexus memo (non-blocking)**: iteration 59 LOW on proto heartbeat/tool facade splits — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 57→0（runtime-host giants / proto config/types / macaca-heartbeat local_provider）；6.3.2 archive；P5 终态验收。

## Iteration 60 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 57→**56**；`macaca-heartbeat/local_provider.rs` 562→172 行；提取 `run_lifecycle.rs`（State 模式 run 生命周期）、`native_cadence.rs`（Command 单 tick 编排）、`completion_sanitizer.rs`（Specification 元数据脱敏）。
- **Design patterns**: Facade module tree（已有 command_handler/gates/memento 基础上扩展）；State（run lifecycle）；Command（native cadence tick）；Specification（completion metadata sanitizer）；trace `info!` on profile register / tick / lifecycle transition。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`macaca-heartbeat` local_provider 5/5。
- **GitNexus memo (non-blocking)**: iteration 60 LOW on heartbeat local_provider facade split — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 56→0（runtime-host giants / proto config/types / scheduled-agent local_provider）；6.3.2 archive；P5 终态验收。

## Iteration 61 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 56→**54**；`skill_service_provider_proposal_materialization.rs` 565→模块树（`mod.rs` Strategy 编排 + `draft_materialization_builder.rs` Builder + `identity_vocabulary.rs` Specification + `content_digest_vocabulary.rs` Value Object）；`application_execution_remote_agent.rs` 567→模块树（`registration.rs` DTO + `transport.rs` Adapter + `registry.rs` Registry + `provider.rs` Strategy）。
- **Design patterns**: Facade module tree；Strategy（materialization + remote provider）；Builder（draft materialization）；Adapter（remote transport）；Registry（remote agent selection）；Specification（identity vocabulary）；Null Object（unavailable transport）；trace `info!`/`warn!` on registration / lease / materialization gates。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`macaca-runtime-host` application_execution_remote_agent 2/2；`skill_proposal_materialization_tests` 4/4。
- **GitNexus memo (non-blocking)**: iteration 61 LOW on runtime-host borderline facade splits — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 54→0（runtime-host giants / proto config/types / heartbeat_lane tests）；6.3.2 archive；P5 终态验收。

## Iteration 62 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 54→**51**；`heartbeat_lane/tests.rs` 571→模块树（`test_doubles.rs` Test Double + `fixtures.rs` Object Mother + `nonblocking_tick`/`cadence_tick`/`run_history` Contract Test）；`default_provider.rs` 532→模块树（`mod.rs` Strategy + `session.rs` State + `artifact_loader.rs` Specification）；`hardened_provider.rs` 574→模块树（`mod.rs` Strategy + `session.rs` Adapter + `response_mapper.rs` Specification）。
- **Design patterns**: Contract Test extraction；Test Double；Object Mother；Strategy（WASM providers）；State（default session）；Adapter（hardened transport/session）；Specification（artifact loader + response mapper）；trace `info!`/`warn!` on session create / dispatch / daemon mapping。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`heartbeat_lane::tests` 4/4；`hardened_provider_tests` 7/7。
- **GitNexus memo (non-blocking)**: iteration 62 LOW on heartbeat/wasm borderline facade splits — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 51→0（runtime-host giants / proto config/types）；6.3.2 archive；P5 终态验收。

## Iteration 63 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 51→**49**；`skill_service_provider_state.rs` 588→模块树（`mod.rs` Facade + `governance_read_model.rs` Read Model + `lifecycle_mutations.rs` State + `alias_governance.rs` + `proposal_store.rs` + `event_vocabulary.rs` Specification）；`lib.rs` 583→265 + `runtime_host_public_api.rs` Facade re-export 面（模块声明与 `pub use` 分离）。
- **Design patterns**: Facade module tree；State（governance lifecycle）；Read Model（snapshots/telemetry）；Specification（event id + snapshot filters）；Strategy bridge（append_event → governance store）；trace `info!`/`warn!`/`debug!` on snapshot/alias/lifecycle/materialization nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`skill_governance_store_logging_tests` 2/2；`skill_service_*` 39/39。
- **GitNexus memo (non-blocking)**: iteration 63 LOW on skill state + runtime-host lib facade splits — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 49→0（runtime-host giants / proto config/types）；6.3.2 archive；P5 终态验收。

## Iteration 64 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 49→**48**；`skill_service_provider.rs` 801→模块树（`mod.rs` Facade + `system_service.rs` + `command_dispatch.rs` Command Router + `runtime_facade_commands.rs` + `operator_commands.rs` + `governance_alias_commands.rs` + `evolution_commands.rs` + `evaluation_content_commands.rs` + `delegated_commands.rs` Strategy bridge）；`skill_sanitization_boundary_tests` `include_str!` 路径同步。
- **Design patterns**: Facade module tree；Command Router；Command handlers per domain；Strategy bridge（curation/lifecycle/materialization delegations）；trace `info!`/`warn!` on command admission/governance/evolution nodes（provider-neutral dimensions only）。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`skill_sanitization_boundary_tests`；`skill_service_provider_tests`。
- **GitNexus memo (non-blocking)**: iteration 64 LOW on skill provider command dispatch split — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 48→0（`skill_service_provider_tests` / runtime-host giants / proto config/types）；6.3.2 archive；P5 终态验收。

## Iteration 65 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 48→**47**；`skill_service_provider_tests.rs` 1811→模块树（`mod.rs` + `fixtures.rs` Object Mother + `test_doubles.rs` Test Double + 10 个按域拆分的 contract test 子模块）；`pub(super)` 可见性修复；恢复 rollback 测试 `#[tokio::test]`。
- **Design patterns**: Contract Test + Object Mother + Test Double（`RecordingMemoryRuntime` 记录 memory/knowledge 路由）。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`skill_service_provider` lib tests 26/26。
- **GitNexus memo (non-blocking)**: iteration 65 LOW on skill provider contract test module split — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 47→0（`mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 66 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 47→**46**；`tool_family_providers.rs` 707→模块树（`mod.rs` Facade + `constants.rs` + `family_catalog.rs` Table Data + `descriptor_builder.rs` Builder + `inventory.rs` + `planning_factory.rs` Abstract Factory）；`tool_service_boundaries` 多文件源扫描同步。
- **Design patterns**: Facade module tree；Abstract Factory（planning contributor/toolsets/service）；Table Data（family catalog）；Builder（descriptor materialization）；Null Object（unsupported-platform availability）；trace `info!`/`debug!` on inventory/contributor/descriptor assembly。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`tool_service_boundaries` 1/1；`tool_service_family_provider` lib tests 5/5。
- **GitNexus memo (non-blocking)**: iteration 66 LOW on tool family provider Abstract Factory split — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 46→0（`mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 67 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 46→**45**；`tool_service_invocation.rs` 678→模块树（`mod.rs` Facade + `admission.rs` Chain of Responsibility + `dispatch.rs` Strategy Router + `helpers.rs` Adapter/Specification）。
- **Design patterns**: Facade module tree；Chain of Responsibility（admission decorators）；Strategy（route-kind dispatch）；Adapter（workbench reply normalization + owning-service payload）；trace `info!`/`warn!` on admission/dispatch/completion nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`tool_service_invocation` lib tests 8/8。
- **GitNexus memo (non-blocking)**: iteration 67 LOW on tool invocation router module split — recorded in `impact-memo.md`.
- **Next**: filesize allowlist 45→0（`mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 68 (2026-06-07)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 45→**44**；`application_execution_hosted.rs` 648→模块树（`mod.rs` Facade + `types.rs` + `abi_adapter.rs` Adapter + `host_signal_translator.rs` Observer + `provider.rs` Strategy/Memento + `provider_impl.rs`）。
- **Design patterns**: Facade module tree；Strategy（`HostedApplicationExecutionAdapter`）；Adapter（ABI bridge）；Observer（host-command signal translation）；Memento（`HostedRunState` cache）；trace `info!`/`warn!` on start/control/shutdown nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`application_execution_hosted` lib tests 6/6；`unified_audit_replay_convergence_tests` 多文件源扫描同步。
- **GitNexus memo (non-blocking)**: iteration 68 HIGH on `application_execution_hosted::*` WASM audit authority paths — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 44→0（`mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 69 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 44→**43**；`host_import_tests.rs` 708→模块树（`mod.rs` Facade + `support.rs` Object Mother + `service_call_tests.rs` + `domain_import_tests.rs` + `audit_genui_tests.rs` + `admission_guard_tests.rs` + `policy_sanitization_tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Object Mother / Test Double（`support.rs`）；Contract Test（按 admission/domain/audit/policy 分区）；Chain of Responsibility 契约（admission guards）；Specification（policy deny + sanitization）。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`wasm_host_import` lib tests 15/15。
- **GitNexus memo (non-blocking)**: iteration 69 LOW on WASM host-import contract test module split — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 43→0（`component_model_tests.rs` / `mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 70 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 43→**42**；`component_model_tests.rs` 652→模块树（`mod.rs` Facade + `support.rs` Object Mother + `descriptor_admission_tests.rs` + `host_import_routing_tests.rs` + `host_command_plan_tests.rs` + `host_command_orchestration_tests.rs` + `lifecycle_timeout_tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Object Mother（portable component fixtures）；Contract Test（descriptor/admission/routing/plan/orchestration/lifecycle 分区）；Template Method（placeholder interpolation）；Chain of Responsibility（declared host-command orchestration）。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`component_model_provider` lib tests 12/12。
- **GitNexus memo (non-blocking)**: iteration 70 LOW on component-model contract test module split — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 42→0（`host_import_bridge.rs` / `mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 71 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 42→**41**；`host_import_bridge.rs` 1154→模块树（`mod.rs` Facade + `constants.rs` + `construction.rs` + `dispatch.rs` + `task_lifecycle.rs` + `ui_render.rs` + `policy_validation.rs` + `result_shaping.rs` + `routing_support.rs` Bridge/Adapter 分区）。
- **Design patterns**: Facade module tree；Bridge（WASM ABI → ServiceRuntime）；Adapter（orchestration payload hydration）；Command（`dispatch` entry）；Repository（GenUI `ui.render` store）；Chain of Responsibility（admission validation）；Specification（policy overrides + sanitization）；trace `info!`/`warn!` on admitted/denied/completed nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`wasm_host_import` lib tests 15/15；`component_model_provider` lib tests 12/12；`unified_audit_replay_convergence` 6/6；`unified_workflow_application_abi` 6/6；多文件 `include_str!` 源扫描同步。
- **GitNexus memo (non-blocking)**: iteration 71 HIGH on `WasmHostImportBridge::dispatch` WASM audit authority path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 40→0（`mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 72 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 41→**40**；`component_model.rs` 818→模块树（`mod.rs` Facade + `provider.rs` Strategy + `session.rs` Session Object + `session_host.rs` Bridge delegation + `session_lifecycle.rs` Memento + `template_engine.rs` Template Method + `support.rs` artifact/dispatch helpers）。
- **Design patterns**: Facade module tree；Strategy（`ComponentModelWasmRuntimeProvider`）；Session Object；Bridge（host-import delegation）；Memento（checkpoint/restore/upgrade/rollback）；Template Method（declared host-command placeholders）；trace `info!`/`warn!` on export invoke / host-command / lifecycle nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`component_model_provider` lib tests 12/12；`wasm_host_import` lib tests 15/15。
- **GitNexus memo (non-blocking)**: iteration 72 HIGH on `ComponentModelWasmExecutionSession::dispatch` WASM export + declared host-command orchestration path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 40→0（`mcp_runtime.rs` / `app_executor.rs` / `mcp_service_provider.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 73 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 40→**39**；`mcp_service_provider.rs` 1048→模块树（`mod.rs` Facade + `descriptor.rs` Factory + `support.rs` Adapter helpers + `system_service.rs` lifecycle + `command_dispatch.rs` Command Router + `runtime_facade_commands.rs` + `tool_invoke_commands.rs` Chain of Responsibility + `operator_commands.rs` + `tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Factory（`mcp_service_descriptor`）；Command Router；Adapter（`McpOperatorLifecycle` / `McpRuntimeFacade` delegation）；Chain of Responsibility（tool invoke admission guards）；Null Object（`unavailable()` provider）；trace `info!`/`warn!` on command admission / rejection / completion nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`mcp_service_provider` lib tests 7/7。
- **GitNexus memo (non-blocking)**: iteration 73 HIGH on `McpSystemServiceProvider::handle_tool_invoke` MCP tool invocation admission + facade side-effect path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 39→0（`mcp_runtime.rs` / `app_executor.rs` / `application_service_provider.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 74 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 39→**38**；`application_service_provider.rs` 1022→模块树（`mod.rs` Facade + `support.rs` Adapter helpers + `wasm_session.rs` Bridge + `genui_surface.rs` Repository + `lifecycle_queries.rs` + `system_service.rs` lifecycle + `command_dispatch.rs` Command Router + `lifecycle_commands.rs` + `session_commands.rs` + `host_commands.rs` Strategy/Bridge + `tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Adapter/Bridge（`macaca-app` delegation + WASM session factory）；Command Router；Repository/Memento（GenUI surface store）；Strategy（`ApplicationOrchestrationBackend` injection）；Null Object（`unavailable()` provider）；trace `info!`/`warn!` on command admission / session / policy sync / delegation nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`application_service_provider` lib tests 4/4；`unified_workflow_application_abi` 6/6（`include_str!` 路径同步至 `command_dispatch.rs`）。
- **GitNexus memo (non-blocking)**: iteration 74 HIGH on `ApplicationSystemServiceProvider::handle_host_dispatch` / `ensure_wasm_session` WASM host dispatch + lazy session creation path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 38→0（`mcp_runtime.rs` / `app_executor.rs` / `autonomy_dispatch.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 75 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 38→**37**；`autonomy_dispatch.rs` 931→模块树（`mod.rs` Facade + `outcome.rs` Value Object + `strategies.rs` Command Router + `agent_execution_dispatch.rs` Strategy/Memento + `service_dispatch.rs` Adapter + `tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Value Object（`AutonomyDispatchOutcome`）；Strategy + Command Router（`AutonomyDispatchStrategies::dispatch`）；Memento（scheduled payload resolve）；Adapter（service/heartbeat syscall）；Specification（`AgentExecutionEvidenceGate`）；trace `info!`/`warn!` on payload resolve / agent execution / evidence verification nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`autonomy_dispatch` lib tests 4/4；`scheduled_agent_task_boundaries` 源扫描路径同步至 `autonomy_dispatch/` 目录。
- **GitNexus memo (non-blocking)**: iteration 75 HIGH on `dispatch_agent_execution` scheduled agent payload resolve + agent execution syscall + evidence gate path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 37→0（`mcp_runtime.rs` / `app_executor.rs` / `autonomy_service_provider.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 76 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 37→**36**；`autonomy_service_provider.rs` 745→模块树（`mod.rs` Facade + `support.rs` Adapter helpers + `scheduler_adapter.rs` + `scheduled_agent_task_adapter.rs` + `heartbeat_adapter.rs` Command Router + `bootstrap.rs` Abstract Factory 分区）。
- **Design patterns**: Facade module tree；Adapter/Bridge（`SystemService` ↔ typed provider DTOs）；Command Router（per-service `call` match tables）；Null Object（`unavailable()` providers）；Abstract Factory（`bootstrap_autonomy_*` composition root）；trace `info!` on command admission / provider register-start / bootstrap completion nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`autonomy_scheduler_heartbeat_services` 11/11。
- **GitNexus memo (non-blocking)**: iteration 76 HIGH on `bootstrap_autonomy_local_services` supervisor start + recovery wake + `HostSchedulerServiceAdapter::call` command router path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 36→0（`mcp_runtime.rs` / `app_executor.rs` / `heartbeat_agent_dispatch.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 77 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 36→**35**；`heartbeat_agent_dispatch.rs` 1023→模块树（`mod.rs` Facade + `summary.rs` Value Object + `support.rs` Specification helpers + `strategy.rs` Strategy/Observer + `tests/{fixtures,contract_tests}.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Value Object（`HeartbeatAgentDispatchSummary`）；Strategy（`HeartbeatAgentDispatchStrategy`）；Observer（`record_completion` → Heartbeat service Memento）；Specification（`declaration_matches_wake` + evidence gate）；Memento（manifest declaration query via Application Service）；trace `info!`/`warn!` on declaration query / dispatch / evidence verification / completion nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`heartbeat_agent_dispatch` lib tests 7/7。
- **GitNexus memo (non-blocking)**: iteration 77 HIGH on `dispatch_after_accepted_wake` / `dispatch_agent` manifest-driven heartbeat → agent execution syscall + evidence gate path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 35→0（`mcp_runtime.rs` / `app_executor.rs` / `fork_manager.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 78 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 35→**34**；`fork_manager.rs` 931→模块树（`mod.rs` Facade + `types.rs` Value Object/State + `manager.rs` + `lifecycle.rs` State transitions + `validation_merge.rs` Specification + `persistence.rs` Memento + `hooks_queries.rs` Observer + `tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；State（`ForkContext` lifecycle memento）；Observer（`HookEvent` broadcast + `emit_hook_event`）；Specification（`validate_result` acceptance criteria）；Memento（`KernelPersistencePort` restore/cleanup）；trace `info!`/`warn!`/`error!` on create/suspend/resume/merge/persistence nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`fork_manager` lib tests 2/2；`executor_runtime_eviction` 5/5；escape-hatch path allowlists 同步至 `fork_manager/mod.rs`。
- **GitNexus memo (non-blocking)**: iteration 78 HIGH on `create_fork` / `resume_fork_by_task` / `merge_fork` fork-join orchestration + persistence restore path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 34→0（`mcp_runtime.rs` / `app_executor.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 79 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 34→**33**；`app_executor.rs` 1401→模块树（`mod.rs` Facade + `types.rs` Value Object + `executor.rs` + `delegation.rs` Template Method + `queries.rs` + `health.rs` + `supervisor.rs` Supervisor + `worker_loop.rs` + `registry.rs` + `tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Supervisor（自动重启 + cooldown 重置）；Template Method（worker-command vs service-backed delegation）；Observer（broadcast executor events + hook forwarder）；Registry（`ApplicationExecutorRegistry`）；Memento（`KernelPersistencePort` queue/fork restore）；trace `info!`/`warn!`/`error!` on delegate/shutdown/supervisor/worker execution nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`app_executor` lib tests 7/7；`executor_runtime_eviction` 5/5；escape-hatch path allowlists 同步至 `app_executor/mod.rs`。
- **GitNexus memo (non-blocking)**: iteration 79 HIGH on `delegate_task` / `supervisor_loop` / `worker_loop` application execution orchestration + fork resume path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 33→0（`mcp_runtime.rs` / proto config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 80 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 33→**32**；`mcp_runtime.rs` 2575→模块树（`mod.rs` Facade + `types.rs` Value Object + `policy.rs` + `facade.rs` + `manager.rs` + `manager_invoke.rs` Template Method + `probe.rs` + `descriptors.rs` + `config_entry.rs` Adapter + `registration.rs` + `client.rs` Strategy + `helpers.rs` + `skill_definitions.rs` + `tests/` Contract Test 分区）。
- **Design patterns**: Facade module tree；Strategy（`McpClientFactory`）；Adapter（YAML/skill→definition）；Registry（invocation + descriptor index）；Template Method（service-backed invoke）；Observer（toolkit `on_close` lease release）；trace `info!`/`warn!`/`error!` on invoke/probe/register/cleanup nodes。
- **Contract tests**: `os_layer_file_size_gate` 1/1；`mcp_runtime` lib tests 22/22；production + test modules all ≤500 lines（max `manager_invoke.rs` 431，`tests/invocation.rs` 318）。
- **GitNexus memo (non-blocking)**: iteration 80 HIGH on `invoke_tool` / `register_definition_tools` / `McpRuntimeFacade::invoke_tool` MCP service-backed tool invocation + descriptor registration path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 32→0（`macaca-proto` config/types 等）；6.3.2 archive；P5 终态验收。

## Iteration 81 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 32→**31**；`config.rs` 1573→`config/` Facade 模块树（`mod.rs` + `root.rs` + `autonomy.rs` + `workspace.rs` + `context/` 子模块 + `kernel.rs` + `llm.rs` + `memory.rs` + `drivers.rs` + `mcp.rs` + `infrastructure.rs` + `builder.rs` + `tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；domain-driven config split；pub re-exports preserve `macaca_proto::config::*` + `MacacaConfigBuilder`/`LlmProviderConfigBuilder` in `lib.rs`。
- **Contract tests**: `cargo test -p macaca-proto config` 10/10；`os_layer_file_size_gate` 1/1；production + test modules all ≤500 lines。
- **GitNexus memo (non-blocking)**: iteration 81 HIGH on `MacacaConfig::load` / `MacacaConfig::load_default` TOML + `AOS_*` env override deserialization path — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 31→0（`macaca-proto` types 等）；6.3.2 archive；P5 终态验收。

## Iteration 82 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 31→**30**；`types.rs` 1514→`types/` Facade 模块树（`mod.rs` + `identity.rs` + `planning.rs` + `agent.rs` + `task.rs` + `todo.rs` + `memory.rs` + `messaging.rs` + `llm.rs` + `execution.rs` + `tests.rs` Contract Test 分区）。
- **Design patterns**: Facade module tree；Visitor（`AgentExecutionEventVisitor` + `accept` dispatch）；Value Object（strongly-typed ID newtypes）；domain-driven split by concern（identity/task/todo/llm/execution）。
- **Contract tests**: `cargo test -p macaca-proto --lib types` 24/24；`os_layer_file_size_gate` 1/1；production + test modules all ≤500 lines（max `tests.rs` 352，`todo.rs` 258）。
- **Escape hatch**: `types/mod.rs` 同步至 `surfaces.rs` + `serviceization_escape_hatches.rs` migration surface allowlist；`agent_execution_service/` 模块树路径同步；Contract Test 提取后暴露的 26 条 raw inventory 债务（`config/tests.rs`、`types/tests.rs` 及跨 crate `tests.rs` fixture）以中性 agent/model/provider 字面量清零（baseline 0 复绿）。
- **GitNexus memo (non-blocking)**: iteration 82 HIGH on `TodoItem` / `AgentExecutionEvent` / `AgentExecutionEventVisitor::accept` cross-crate domain serde + execution progress dispatch — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 30→0（`macaca-framework` / `agentic_loop` / `application_service` 等）；6.3.2 archive；P5 终态验收。

## Iteration 83 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 30→**29**；`application_service.rs` 684→`application_service/` Facade 模块树（`mod.rs` + `constants.rs` + `scope.rs` + `lifecycle_commands.rs` + `delegate.rs` + `query_commands.rs` + `ui_bridge.rs` + `app_views.rs` + `metadata_views.rs` + `snapshot.rs` + `validation.rs`）。
- **Design patterns**: Facade module tree；Command（lifecycle/query/delegate DTOs）；DTO/View（sanitized metadata + heartbeat/runtime views）；Memento（`ApplicationServiceSnapshot`）；Adapter（`into_service_command` bridge）。
- **Contract tests**: `cargo check -p macaca-proto` OK；`os_layer_file_size_gate` 1/1；`unified_workflow_application_abi` `include_str!` 路径同步至 `application_service/delegate.rs`；production modules all ≤500 lines（max `lifecycle_commands.rs` 110）。
- **GitNexus memo (non-blocking)**: iteration 83 HIGH on `ApplicationAgentDelegateCommand::into_service_command` + Application Service wire DTO serde surface — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 29→0（`macaca-framework` / `agentic_loop` / `todo_board.rs` 等）；6.3.2 archive；P5 终态验收。

## Iteration 84 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 29→**28**；`plan_loop.rs` 687→`plan_loop/` Facade 模块树（`mod.rs` + `config.rs` + `events.rs` + `loop_runner.rs` + `goal_evaluator.rs` + `tests.rs`）。
- **Design patterns**: Facade module tree；Observer（`PlanEvent` channel decouples scheduler from LLM consumers）；Template Method（`run_with_default_template`）；Strategy（`GoalEvaluator` pluggable completion policy）；Value Object（`PlanLoopConfig`, `TaskSummary`, `GoalEvaluation`）。
- **Contract tests**: `cargo check -p macaca-task` OK；`cargo test -p macaca-task plan_loop` OK；`os_layer_file_size_gate` 1/1；`serviceization_escape_hatches` OK；production modules all ≤500 lines（max `loop_runner.rs` ~288）。
- **Escape hatch**: `plan_loop/mod.rs` 同步至 `surfaces.rs` + `serviceization_escape_hatches.rs` migration surface allowlist；Contract Test fixture 中性化（`backend`→`agent-alpha`，`architect`→`agent-beta`）。
- **GitNexus memo (non-blocking)**: iteration 84 HIGH on `PlanLoop::run` heartbeat dispatch + `GoalEvaluator::evaluate` goal-completion seam — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 28→0（`macaca-framework` / `agentic_loop` / `todo_board.rs` / `runtime.rs` 等）；6.3.2 archive；P5 终态验收。

## Iteration 85 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 28→**27**；`macaca-skill/runtime.rs` 692→`runtime/` Facade 模块树（`mod.rs` + `config.rs` + `types.rs` + `discovery.rs` + `filter.rs` + `prompt.rs` + `projection.rs` + `path_policy.rs` + `tests.rs`）。
- **Design patterns**: Facade module tree；Memento（`SkillSnapshot`）；Chain of Responsibility（`SkillPolicyChain`）；Strategy（full/compact prompt + source precedence）；Value Object（`SkillRuntimeOptions`, limits, snapshot entries）。
- **Contract tests**: `cargo check -p macaca-skill` OK；`cargo test -p macaca-skill runtime::` 5/5；`os_layer_file_size_gate` 1/1；`serviceization_escape_hatches` OK；production modules all ≤500 lines（max `discovery.rs` 170）。
- **Logging**: `tracing::info/debug/warn` at snapshot build, discovery limits, policy counts, prompt truncation, workspace projection.
- **GitNexus memo (non-blocking)**: iteration 85 HIGH on `SkillRuntime::build_snapshot` + `path_belongs_to_snapshot_skill` file-policy seam — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 27→0（`macaca-task/runtime.rs` / `todo_board.rs` / `agentic_loop` 等）；6.3.2 archive；P5 终态验收。

## Iteration 86 (2026-06-08)
- **P5 §4.5.1 / §6.1.6 filesize 子阶段**: 边界文件 27→**26**；`agent_skill.rs` 676→`agent_skill/` Facade 模块树（`mod.rs` + `scope.rs` + `model.rs` + `metadata.rs` + `parser.rs` + `tests.rs`）。
- **Design patterns**: Facade module tree；Value Object（`AgentSkill`, metadata DTOs, `SkillSourceScope`）；Builder/Parser（`parser` → `ParsedSkillMd`）；Lazy Loading（tier-1 metadata vs tier-2 `ActivatedSkill`）。
- **Contract tests**: `cargo check -p macaca-skill` OK；`cargo test -p macaca-skill agent_skill::` 11/11；`os_layer_file_size_gate` 1/1；production modules all ≤500 lines（max `parser.rs` ~230）。
- **Logging**: `tracing::info/debug` at tier-1 load and tier-2 activation; contract test MCP fixture neutralized (`playwright`→`sample-mcp`).
- **GitNexus memo (non-blocking)**: iteration 86 HIGH on `AgentSkill::from_path_with_source` + `parse_skill_md_full` cross-crate SKILL.md serde surface — recorded in `impact-memo.md` (memo only per user directive)。
- **Next**: filesize allowlist 26→0（`macaca-task/runtime.rs` / `todo_board.rs` / `agentic_loop` 等）；6.3.2 archive；P5 终态验收。
