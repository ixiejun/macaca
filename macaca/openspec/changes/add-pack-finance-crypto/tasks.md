## 1. Supplier API Research And Scope

- [x] 1.1 Re-read architecture governance, microkernel boundaries, serviceization allowlist, design patterns, OpenSpec rules, and the industrial catalog umbrella proposal before implementation.
- [x] 1.2 Study CoinGecko APIs for coins, asset metadata, market data, historical charts, exchanges, categories, NFT/reference data, rate limits, and errors.
- [x] 1.3 Study CoinMarketCap APIs for listings, quotes, metadata, categories, exchange data, market pairs, plan credits, and attribution behavior.
- [x] 1.4 Study Coinbase, Kraken, Binance, and OKX market-data APIs for exchange products/pairs, ticker, candles/klines, trades, status, precision, rate limits, and errors.
- [x] 1.5 Study Etherscan-like explorer APIs, Chainlink feed documentation, and DefiLlama datasets as baselines for read-only token metadata, address balance references, oracle/feed references, DeFi reference metrics, and chain-specific freshness.
- [x] 1.6 Study CCXT and exchange abstraction patterns as reference only for symbol normalization, pair mapping, rate limits, and capability discovery, without adopting provider-specific runtime semantics.
- [x] 1.7 Produce a supplier capability comparison memo mapping aggregator/exchange/explorer/oracle/cache concepts into Macaca provider-neutral crypto DTOs and commands.
- [x] 1.8 Define explicit non-goals for trading, exchange order routing, wallet custody, private key management, signing, transfer intent, swap intent, staking, bridge, DeFi execution, portfolio holdings, raw chain payload observability, and provider/exchange/chain/token-specific routing.
- [x] 1.9 Record GitNexus CRITICAL/HIGH findings as memo only before implementation commits.

## 2. Contract, Descriptor, And DTOs

- [x] 2.1 Define `pack.finance.crypto.v1` descriptor metadata: pack id, family, lifecycle, stability, supported asset classes, chains, exchanges, market pairs, quote/trade/bar/snapshot support, supply metric support, token reference support, public address diagnostics support, oracle/feed support, pagination model, attribution requirements, auth modes, rate limits, command schemas, permission scopes, policy templates, resource budgets, approval requirements, data-governance class, SDK metadata, documentation link, compatibility, and diagnostics.
- [x] 2.2 Define provider-neutral DTOs for `CryptoScope`, `CryptoProviderCapability`, `CryptoAsset`, `TokenReference`, `ChainNetworkReference`, `CryptoExchangeVenue`, `CryptoMarketPair`, `CryptoQuote`, `CryptoTrade`, `CryptoBar`, `CryptoBarSeries`, `CryptoSnapshot`, `CryptoSupplyMetric`, `PublicAddressBalanceReference`, `CryptoOracleFeedReference`, `CryptoFreshness`, `CryptoAttribution`, `CryptoCursor`, and `CryptoArtifactHandle`.
- [x] 2.3 Define typed command/result DTOs for `crypto.inspect_provider`, `crypto.search_assets`, `crypto.get_asset`, `crypto.get_token_reference`, `crypto.search_market_pairs`, `crypto.get_quote`, `crypto.get_trade`, `crypto.get_bars`, `crypto.get_snapshot`, `crypto.get_supply_metrics`, `crypto.inspect_market_status`, `crypto.inspect_public_address_balance`, `crypto.inspect_freshness`, and `crypto.get_artifact_handle`.
- [x] 2.4 Define typed success, paged, partial, denied, unavailable, unsupported, conflict, stale-data, schema-mismatch, provider-attribution-required, license-denied, asset-ambiguous, asset-not-found, chain-unsupported, exchange-unsupported, pair-unsupported, interval-unsupported, address-policy-denied, web3-unavailable, range-too-large, quota, timeout, cancellation, and failure DTOs.
- [x] 2.5 Define stable descriptor hashing, provider capability hashing, asset identity hashing, token reference hashing, chain/network hashing, venue/pair hashing, quote/trade/bar/snapshot hashing, supply metric hashing, public address reference hashing, oracle feed hashing, freshness hashing, attribution hashing, cursor hashing, artifact handle hashing, event cursor hashing, and redaction metadata.
- [x] 2.6 Add descriptor and DTO compatibility tests for valid descriptors, rejected invalid descriptors, stable hashes, schema evolution, assets, tokens, chains, venues, pairs, quotes, trades, bars, snapshots, supply metrics, address references, oracle feeds, freshness, attribution, cursors, artifacts, and serde compatibility.

## 3. Admission, Permission, Policy, Resource, Entitlement, And Privacy

- [x] 3.1 Implement manifest declaration validation for required and optional `pack.finance.crypto.v1` declarations.
- [x] 3.2 Implement permission validation for `crypto.provider.inspect`, `crypto.asset.search`, `crypto.asset.read`, `crypto.token.read`, `crypto.market_pair.search`, `crypto.quote.read`, `crypto.trade.read`, `crypto.bars.read`, `crypto.snapshot.read`, `crypto.supply.read`, `crypto.market_status.read`, `crypto.public_address.read`, `crypto.freshness.read`, and `crypto.artifact.read`.
- [ ] 3.3 Implement provider/asset/token/chain/exchange/pair/address/cursor/artifact scope checks for declared providers, exchanges, chains, tokens, public address diagnostics, premium datasets, denied scopes, stale handles, and redistribution-sensitive data.
- [ ] 3.4 Implement policy checks for asset search bounds, token reference resolution, contract address hashing, chain/network support, exchange/venue support, pair support, quote currency, date range, interval, pagination, address privacy policy, freshness policy, attribution requirements, cache policy, and output redaction.
- [ ] 3.5 Implement resource reservation for result size, historical range, page count, asset count, pair count, bar count, address reference count, provider quota, chain/explorer quota, network transfer, timeout, memory, storage, and retained snapshots.
- [ ] 3.6 Implement entitlement and license checks with structured unavailable/denied diagnostics for missing provider, disabled pack, missing credential reference, missing permission, missing exchange entitlement, missing chain/explorer entitlement, address-policy denial, unsupported chain, unsupported pair, provider quota, stale cache, optional Web3 absence, and host resource denial.
- [ ] 3.7 Implement approval behavior for public address diagnostics, premium datasets, exchange-licensed data, chain-indexed data, redistribution-sensitive data, region-restricted data, very large historical requests, or external-publication requests.
- [ ] 3.8 Add tests proving denied, validation, quota, unavailable, conflict, stale-data, schema-mismatch, attribution-required, license-denied, asset-ambiguous, asset-not-found, chain-unsupported, exchange-unsupported, pair-unsupported, interval-unsupported, address-policy-denied, web3-unavailable, range-too-large, timeout, and cancellation paths do not call concrete providers, sign transactions, create transfers, trade, leak address data, leak chain payloads, strip attribution, or expose unbounded datasets.

## 4. Service Provider And Runtime Integration

- [ ] 4.1 Implement or bind the crypto finance service provider behind the service runtime; do not construct crypto providers from SDK, shell, kernel, or application code.
- [x] 4.2 Add a deterministic unavailable provider that returns typed unavailable/unsupported diagnostics and complete discovery metadata.
- [ ] 4.3 Add mock provider support for provider inspection, asset search, asset metadata, token references, chain/network references, market pairs, quotes, trades, bars, snapshots, supply metrics, market status, public address diagnostics, freshness inspection, artifact handles, health, and provider capability inspection.
- [ ] 4.4 Add lifecycle, health, snapshot, shutdown, timeout, cancellation, bounded paging, cache/artifact behavior, stale-data diagnostics, provider attribution diagnostics, schema/asset/chain/exchange/pair mismatch diagnostics, address privacy diagnostics, license diagnostics, optional Web3 absence diagnostics, and rate-limit diagnostics.
- [ ] 4.5 Add Strategy implementations for provider adapters, aggregator readers, exchange readers, explorer readers, oracle readers, token metadata readers, supply metric readers, address diagnostics readers, freshness resolvers, attribution resolvers, cache readers, redaction, and unavailable behavior.
- [ ] 4.6 Add side-effect safety support for cache-producing reads: request hashes, provider state validation, entitlement state, license state, attribution state, freshness state, address privacy state, cursor validation, artifact retention, and bounded replay.
- [ ] 4.7 Add provider capability reporting for available, degraded, preview, unavailable, unsupported, retired, exchange-limited, chain-limited, quote-limited, history-limited, supply-limited, address-diagnostics-limited, oracle-limited, license-limited, attribution-required, network-limited, and quota-limited states.

## 5. SDK, WASM ABI, Application Framework, And Examples

- [x] 5.1 Extend SDK discovery for `pack.finance.crypto.v1` with command schemas, supported assets/chains/exchanges, market-pair support, quote/trade/bar/snapshot support, supply metric support, token reference support, address diagnostics support, oracle/feed support, attribution requirements, freshness classes, examples, availability, diagnostics, documentation link, provider class, capability hash, compatibility, and redaction profile.
- [x] 5.2 Extend application admission so required declarations block readiness when unavailable and optional declarations degrade explicitly with effective capability mementos.
- [x] 5.3 Add SDK command helper builders for all `crypto.*` commands; helpers must only build canonical traced service calls and must never construct crypto clients, access credentials, access private keys, sign transactions, create transfers, query providers directly, strip attribution, hide freshness, infer advice, trade, or bypass policy.
- [ ] 5.4 Extend WASM/app ABI descriptors so applications can discover crypto commands, declare permissions, receive unavailable diagnostics, and submit typed service calls through the canonical execution path.
- [x] 5.5 Add generic app-facing examples for provider inspection, asset search, asset metadata, token references, market pairs, quotes, trades, bars, snapshots, supply metrics, market status, public address diagnostics, freshness, artifact handles, and unavailable diagnostics.
- [x] 5.6 Add unavailable-provider, missing-entitlement, license-denied, stale-data, asset-ambiguous, asset-not-found, chain-unsupported, exchange-unsupported, pair-unsupported, interval-unsupported, address-policy-denied, web3-unavailable, attribution-required, provider-quota, network-denied, and artifact-denied examples using synthetic data only.

## 6. Trace, Audit, Replay, Security, And Gates

- [ ] 6.1 Emit sanitized declaration, admission, provider-inspection, asset-search, asset-read, token-reference-read, market-pair-search, quote-read, trade-read, bars-read, snapshot-read, supply-read, market-status, public-address-diagnostics, freshness, artifact-handle, policy, entitlement, license, resource, privacy, health, snapshot, unavailable, and failure events.
- [ ] 6.2 Ensure traces, audits, snapshots, SDK diagnostics, and examples exclude raw credentials, private keys, seed phrases, signatures, user holdings, raw provider payloads, licensed feed payloads, raw chain payloads, manifests, package bytes, and unbounded crypto datasets.
- [ ] 6.3 Add replay tests proving every `crypto.*` command is trace-addressable through the canonical service path and that snapshots contain enough bounded metadata for recovery diagnostics.
- [ ] 6.4 Add dependency gates proving kernel, SDK, shells, and generic application framework do not import concrete CoinGecko, CoinMarketCap, Coinbase, Kraken, Binance, OKX, Etherscan, Chainlink, DefiLlama, CCXT, exchange, explorer, wallet, signing, chain client, cache, entitlement, credential-manager, or provider adapters.
- [ ] 6.5 Add no-direct-provider-call gates proving SDK helpers, WASM ABI handlers, app admission, web, CLI, and frontend paths route through descriptor-owned service commands.
- [ ] 6.6 Add boundary tests proving optional provider absence returns structured unavailable diagnostics and never crashes, hangs, silently falls back, fabricates crypto prices, signs transactions, creates transfers, calls providers, strips freshness/attribution, leaks addresses or chain payloads, or fakes success.
- [ ] 6.7 Run `openspec validate add-pack-finance-crypto --strict`, targeted cargo tests, boundary gates, file-size gates, and audit replay checks before marking implementation complete.

## 7. Developer Documentation

- [x] 7.1 Create `docs/developer-packs/finance/crypto.md` with purpose, capability model, manifest declaration, required versus optional behavior, permissions, provider scopes, crypto assets, token references, chain/network references, exchange venues, market pairs, quotes, trades, bars, snapshots, supply metrics, public address balance references, oracle/feed references, freshness, attribution, entitlements, licenses, cursors, artifacts, unavailable diagnostics, provider replacement, and operational limits.
- [x] 7.2 Document all command DTOs and result DTOs with field-level explanations, pagination semantics, cache semantics, redaction behavior, timeout/cancellation behavior, attribution behavior, license behavior, address privacy behavior, freshness classes, stale-data diagnostics, chain height/finality, quote currency handling, interval/range limits, artifact retention, and structured error codes.
- [x] 7.3 Document supplier/API mapping: CoinGecko, CoinMarketCap, Coinbase, Kraken, Binance, OKX, Etherscan-like explorers, Chainlink, DefiLlama, CCXT, exchange direct-feed, cache, entitlement, market-data handoff, optional Web3 handoff, and attribution concepts mapped to Macaca abstractions, including what is intentionally not exposed as OS semantics.
- [x] 7.4 Add generic examples for provider inspection, asset search, asset metadata, token reference, market pairs, quote, trade, bars, snapshot, supply metric, market status, public address diagnostics, freshness, artifact handles, stale diagnostics, and unavailable diagnostics using synthetic crypto data only.
- [x] 7.5 Add conformance checklist and test guidance for provider authors: descriptor completeness, asset/token/chain/exchange/pair scope validation, quote/trade/bar validation, supply metric validation, public address privacy enforcement, freshness labeling, attribution enforcement, license checks, pagination, resource bounds, policy hooks, trace/audit events, unavailable behavior, snapshot/replay, and redaction.
- [x] 7.6 Cross-link the guide from SDK discovery metadata and the industrial pack catalog index before marking `add-pack-finance-crypto` complete.
