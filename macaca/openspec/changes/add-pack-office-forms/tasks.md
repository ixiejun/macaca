## 1. Supplier API Research And Scope

- [x] 1.1 Re-read architecture governance, microkernel boundaries, serviceization allowlist, design patterns, OpenSpec rules, and the industrial catalog umbrella proposal before implementation.
- [x] 1.2 Study Google Forms API for create/get, `forms.batchUpdate`, publish settings, response get/list, and watches for schema and response events.
- [x] 1.3 Study Typeform Create API, Responses API, and Webhooks API for form schema creation/update, themes/images, response retrieval, webhook delivery, and event signing behavior.
- [x] 1.4 Study Jotform API for account/form/submission access, response/submission retrieval patterns, and webhook notification behavior.
- [x] 1.5 Study Microsoft Graph/Microsoft Forms official capability gaps and model missing Forms response APIs as structured provider unsupported/unavailable states rather than compatibility assumptions.
- [x] 1.6 Study HTML/JSON Schema and common form-rendering concepts only as neutral schema inspiration; do not expose one renderer's field model as OS semantics.
- [x] 1.7 Produce a supplier capability comparison memo mapping Google Forms, Typeform, Jotform, Microsoft Forms/Graph gaps, webhook providers, and renderer concepts into Macaca provider-neutral forms DTOs and commands.
- [x] 1.8 Define explicit non-goals for concrete provider implementation, survey/HR/medical/legal/school/CRM/quiz/order/lead-gen workflows, raw provider pass-through, raw response observability, and provider-specific routing.
- [x] 1.9 Record GitNexus CRITICAL/HIGH findings as memo only before implementation commits.

## 2. Contract, Descriptor, And DTOs

- [x] 2.1 Define `pack.office.forms.v1` descriptor metadata: pack id, family, lifecycle, stability, create/open/import support, schema-edit support, field-type support, validation support, conditional-logic support, publish support, response validation/submission support, response read/export support, webhook/watch support, auth modes, command schemas, permission scopes, policy templates, resource budgets, approval requirements, data-governance class, SDK metadata, documentation link, compatibility, and diagnostics.
- [x] 2.2 Define provider-neutral DTOs for `FormsScope`, `FormsProviderCapability`, `FormHandle`, `FormMetadata`, `FormSchema`, `FormSection`, `FormField`, `FormFieldOption`, `FormValidationRule`, `FormLogicRule`, `FormPublishSettings`, `RespondentSession`, `FormResponseDraft`, `FormResponseValue`, `FormSubmissionReceipt`, `FormResponseExportPlan`, `FormEventSubscriptionPlan`, `FormEvent`, and `FormArtifactHandle`.
- [x] 2.3 Define typed command/result DTOs for `forms.inspect_provider`, `forms.create_form_request`, `forms.import_form_request`, `forms.open_form`, `forms.inspect_metadata`, `forms.inspect_schema`, `forms.plan_schema_edit`, `forms.schema_edit_request`, `forms.create_response_session`, `forms.validate_response_draft`, `forms.submit_response_request`, `forms.get_submission_receipt`, `forms.list_responses`, `forms.get_response`, `forms.plan_response_export`, `forms.response_export_request`, `forms.plan_event_subscription`, `forms.event_subscription_request`, `forms.inspect_events`, and `forms.get_artifact_handle`.
- [x] 2.4 Define typed success, paged, partial, asynchronous, denied, unavailable, unsupported, conflict, stale-version, schema-mismatch, validation-failed, publish-denied, submit-denied, export-denied, webhook-denied, webhook-signature-invalid, response-redacted, quota, timeout, cancellation, approval-required, and failure DTOs.
- [x] 2.5 Define stable descriptor hashing, provider capability hashing, form version hashing, schema version hashing, field/validation/logic compatibility hashing, response draft hashing, submission receipt hashing, response export plan hashing, event subscription plan hashing, event cursor hashing, artifact handle hashing, and redaction metadata.
- [x] 2.6 Add descriptor and DTO compatibility tests for valid descriptors, rejected invalid descriptors, stable hashes, schema evolution, forms, schemas, sections, fields, options, validation rules, logic rules, publish settings, respondent sessions, response drafts, response values, submission receipts, exports, event subscriptions, events, artifacts, redaction profiles, and serde compatibility.

## 3. Admission, Permission, Policy, Resource, Entitlement, And Approval

- [x] 3.1 Implement manifest declaration validation for required and optional `pack.office.forms.v1` declarations.
- [ ] 3.2 Implement permission validation for `forms.provider.inspect`, `forms.form.create`, `forms.form.import`, `forms.form.open`, `forms.metadata.read`, `forms.schema.read`, `forms.schema.write`, `forms.response.session`, `forms.response.validate`, `forms.response.submit`, `forms.response.read`, `forms.response.export`, `forms.event.subscribe`, `forms.event.read`, and `forms.artifact.read`.
- [ ] 3.3 Implement provider/form/schema/field/response/respondent/export/event/artifact scope checks for declared forms, private forms, published forms, restricted respondent groups, PII fields, denied scopes, stale handles, and webhook endpoints.
- [ ] 3.4 Implement policy checks for schema compatibility, field type support, validation rules, conditional logic, publish state, respondent session expiration, consent metadata, response validation, idempotent submission, response read/export filters, PII redaction, webhook signing, event subscription renewal, artifact retention, and output redaction.
- [ ] 3.5 Implement resource reservation for form count, schema size, section count, field count, option count, validation rule count, logic rule count, response value count, response count, response export size, webhook event rate, artifact size, provider quota, network transfer, timeout, memory, storage, streaming output, and retained snapshots.
- [ ] 3.6 Implement entitlement checks and structured unavailable/denied diagnostics for missing provider, disabled pack, missing credential reference, missing form permission, unsupported schema/field/validation/logic/response/export/webhook support, disabled network, missing entitlement, provider quota, and host resource denial.
- [ ] 3.7 Implement approval behavior for public publish changes, PII collection, regulated answers, respondent identifiers, response export, external webhook subscription, destructive schema edits, external notifications, and operations that publish artifacts or send data outside the tenant boundary.
- [ ] 3.8 Add tests proving denied, validation, quota, unavailable, conflict, stale-version, schema-mismatch, validation-failed, publish-denied, submit-denied, export-denied, webhook-denied, webhook-signature-invalid, response-redacted, unsupported, timeout, cancellation, and approval-required paths do not call concrete providers, publish forms, mutate schemas, submit duplicate responses, export responses, create webhooks, notify external systems, or expose raw response data.

## 4. Service Provider And Runtime Integration

- [ ] 4.1 Implement or bind the forms service provider behind the service runtime; do not construct forms providers from SDK, shell, kernel, or application code.
- [x] 4.2 Add a deterministic unavailable provider that returns typed unavailable/unsupported diagnostics and complete discovery metadata.
- [ ] 4.3 Add mock provider support for provider inspection, create/import/open, metadata inspection, schema inspection, schema edit planning/request, response session creation, response draft validation, response submission, receipt retrieval, response listing/retrieval, response export planning/request, event subscription planning/request, event inspection, artifact handles, health, and provider capability inspection.
- [ ] 4.4 Add lifecycle, health, snapshot, shutdown, timeout, cancellation, bounded paging, asynchronous export behavior, schema version freshness, stale-version diagnostics, schema mismatch diagnostics, validation diagnostics, webhook signature diagnostics, artifact retention, and rate-limit diagnostics.
- [ ] 4.5 Add Strategy implementations for provider adapters, schema validators, response validators, publish-state managers, export providers, webhook/event verifiers, event subscription providers, artifact providers, redaction providers, and unavailable behavior.
- [ ] 4.6 Add side-effect safety support for idempotency keys, provider state validation, schema version preconditions, response session expiration, consent metadata, approval state, webhook signing policy, artifact retention, and non-mutating plan/validate commands.
- [ ] 4.7 Add provider capability reporting for available, degraded, preview, unavailable, unsupported, retired, schema-limited, validation-limited, logic-limited, response-limited, export-limited, webhook-limited, network-limited, and quota-limited states.

## 5. SDK, WASM ABI, Application Framework, And Examples

- [x] 5.1 Extend SDK discovery for `pack.office.forms.v1` with command schemas, field-type support, validation support, logic support, publish support, response submission support, response read/export support, webhook/watch support, examples, availability, diagnostics, documentation link, provider class, capability hash, compatibility, and redaction profile.
- [x] 5.2 Extend application admission so required declarations block readiness when unavailable and optional declarations degrade explicitly with effective capability mementos.
- [x] 5.3 Add SDK command helper builders for all `forms.*` commands; helpers must only build canonical traced service calls and must never construct forms clients, access credentials/webhook secrets, call provider APIs, publish forms, mutate schemas, submit responses, export raw responses, create webhooks, or bypass policy.
- [ ] 5.4 Extend WASM/app ABI descriptors so applications can discover forms commands, declare permissions, receive unavailable diagnostics, and submit typed service calls through the canonical execution path.
- [x] 5.5 Add generic app-facing examples for provider inspection, create/import/open, metadata inspection, schema inspection, schema edit planning/request, response session creation, draft validation, response submission, receipt retrieval, response listing/retrieval, response export planning/request, event subscription planning/request, event inspection, and artifact handles.
- [x] 5.6 Add unavailable-provider, missing-form-permission, stale-schema-version, schema-mismatch, validation-failed, publish-denied, submit-denied, duplicate-idempotency, response-redacted, export-approval, webhook-denied, webhook-signature-invalid, provider-quota, network-denied, and artifact-denied examples that demonstrate diagnostics without provider names, credentials, webhook secrets, private respondent data, raw responses, raw exports, or workflow-specific conventions.

## 6. Trace, Audit, Replay, Security, And Gates

- [ ] 6.1 Emit sanitized declaration, admission, provider-inspection, create/import/open, metadata-inspection, schema-inspection, schema-edit-plan/request, response-session, draft-validation, response-submission, receipt, response-list/get, response-export-plan/request, event-subscription-plan/request, event-inspection, artifact-handle, policy, entitlement, resource, approval, health, snapshot, unavailable, and failure events.
- [ ] 6.2 Ensure traces, audits, snapshots, SDK diagnostics, and examples exclude raw credentials, webhook secrets, respondent PII, raw responses, raw exports, raw provider payloads, prompts, manifests, package bytes, private keys, signatures, and unbounded response sets.
- [ ] 6.3 Add replay tests proving every `forms.*` command is trace-addressable through the canonical service path and that snapshots contain enough bounded metadata for recovery diagnostics.
- [ ] 6.4 Add dependency gates proving kernel, SDK, shells, and generic application framework do not import concrete Google Forms, Typeform, Jotform, Microsoft Forms, spreadsheet/export, notification, webhook transport, credential-manager, artifact-provider, or provider adapters.
- [ ] 6.5 Add no-direct-provider-call gates proving SDK helpers, WASM ABI handlers, app admission, web, CLI, and frontend paths route through descriptor-owned service commands.
- [ ] 6.6 Add boundary tests proving optional provider absence returns structured unavailable diagnostics and never crashes, hangs, silently falls back, publishes forms, mutates schemas, submits responses, exports responses, creates webhooks, notifies external systems, contacts providers, or fakes success.
- [ ] 6.7 Run `openspec validate add-pack-office-forms --strict`, targeted cargo tests, boundary gates, file-size gates, and audit replay checks before marking implementation complete.

## 7. Developer Documentation

- [x] 7.1 Create `docs/developer-packs/office/forms.md` with purpose, capability model, manifest declaration, required versus optional behavior, permissions, provider scopes, form handles, metadata, schemas, sections, fields, options, validation rules, conditional logic, publish settings, respondent sessions, response drafts, response values, submission receipts, response exports, webhook/event subscriptions, event cursors, artifacts, unavailable diagnostics, provider replacement, and operational limits.
- [x] 7.2 Document all command DTOs and result DTOs with field-level explanations, idempotency semantics, redaction behavior, pagination/streaming/asynchronous export behavior, timeout/cancellation behavior, plan/request behavior, approval behavior, artifact retention behavior, schema version preconditions, validation behavior, consent metadata, webhook signing policy, event renewal behavior, and structured error codes.
- [x] 7.3 Document supplier/API mapping: Google Forms API, Typeform Create/Responses/Webhooks APIs, Jotform API, Microsoft Forms/Graph gaps, webhook providers, and renderer concepts mapped to Macaca abstractions, including what is intentionally not exposed as OS semantics.
- [x] 7.4 Add generic examples for provider inspection, form create/import/open, schema inspection, schema edit, response session, draft validation, submission, receipts, response read/export, webhook/event subscriptions, event inspection, artifact handles, and unavailable diagnostics using synthetic form data only.
- [x] 7.5 Add conformance checklist and test guidance for provider authors: descriptor completeness, form/schema/field/response scope validation, schema compatibility, validation logic, conditional logic, publish state, respondent sessions, idempotent submissions, PII redaction, response export validation, webhook signature validation, event cursor handling, artifact redaction, resource bounds, policy hooks, trace/audit events, unavailable behavior, snapshot/replay, and redaction.
- [x] 7.6 Cross-link the guide from SDK discovery metadata and the industrial pack catalog index before marking `add-pack-office-forms` complete.
