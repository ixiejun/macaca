# Tools

Use `file_read` to inspect source packs and drafts.

Use `playwright_search` to verify questionable claims or fetch missing primary sources.

Use `file_write` to save fact-check notes under `shared/research/`.
