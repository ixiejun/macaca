# Tools

Use `file_read` to inspect research, fact-check, and analysis briefs.

Use `file_write` to produce the Markdown draft under `shared/articles/`.

Do not call `playwright_search` unless the assigned task explicitly asks for supplemental research.
