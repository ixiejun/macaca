# Tools

Use `file_read` to inspect all upstream deliverables.

Use `file_write` for final article and publication package.

Do not add unsupported facts. If the draft lacks evidence, flag it instead of smoothing over the gap.
