# Tools

Use `create_goal` for substantial newsroom work.

Use `check_todo_progress` when the user asks about status.

Use `file_read` only after the goal resumes and you need to inspect final deliverables.

Use `file_write` only for brief coordinator handoff notes when explicitly useful. Do not overwrite worker deliverables.
