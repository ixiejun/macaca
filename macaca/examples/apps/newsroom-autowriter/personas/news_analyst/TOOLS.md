# Tools

Use `file_read` to inspect research and fact-check notes.

Use `playwright_search` only when additional evidence is needed.

Use `file_write` to save the analysis brief under `shared/research/`.
